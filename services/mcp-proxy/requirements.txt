# MCP Proxy — pure MCP wire client. NEVER add sqlalchemy/asyncpg: the proxy is
# deliberately kept off the DB and off AGENTSHIELD_ENCRYPTION_KEY (design §3b/§3c,
# research.md B3/B13). Connection + credentials come only from per-server K8s Secrets.
fastapi
uvicorn[standard]
pydantic
httpx
kubernetes
# Official Model Context Protocol Python SDK. Pin <2.0 — 2.0 renames the client
# (streamablehttp_client -> streamable_http_client) and FastMCP -> MCPServer and
# moves to httpx2 (research.md B1). Re-verify the exact stable minor at build:
#   pip index versions mcp
mcp>=1.2,<2.0
