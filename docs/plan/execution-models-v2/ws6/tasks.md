# WS-6 Tasks — Operate-surface parity: one pod-URL resolver, one Overview dispatch, one build marker

**Slice:** WS-6 of Execution Models v2 (spec §5 WS-6; absorbs the five items moved out of `execution-models-gap-analysis.md` on 2026-07-12; plan `ws6/plan.md`). **Off the spine** — needs WS-1 only for the `/approvals` inbox page the nav badge routes to (**already shipped**: `App.tsx:87` → `ApprovalsInboxPage`), so WS-6 is standalone today.

**Total tasks:** 29 (23 implementation + 6 checkpoint)
**Phases:** 9 (7 implementation + 2 checkpoint gates)
**Parallel opportunities:** noted inline with `[P]`
**Checkpoint phases:** **Checkpoint 1** (after Phase 3 — one env-aware pod-URL resolver, proven by a **real** sandbox approval driven to timeout through the **real** worker; **this is the MVP gate**), **Checkpoint 2** (after Phase 7 — Overview dispatch convergence + nav badge + build-marker enforcement + full suite-79 + post-impl gates).

> ⚠️ **Re-grounded against the live tree (2026-07-15).** The plan's specifics were **indicative against the 2026-07-12 tree** and it warns they drift (plan §status). **Two of the plan's six items are already shipped, and one must be rejected outright.** Every correction below was verified by reading the live file — **use these, not the plan's numbers**:
>
> | Plan says | Live truth (verified 2026-07-15) |
> |---|---|
> | suite `61` | **TAKEN** — `scripts/e2e/suite-61-eval-mode-plumbing.sh`. Suites **1..77 all exist**; **78 is WS-5's** (being written concurrently). WS-6 = **`suite-79-operate-parity.sh`**, test IDs **`T-S79-00x`**, registered **after suite-77** (`run-all.sh:126`). |
> | migration: **none** | **CORRECT — the plan's one clean call.** Alembic head = **`0064_webhook_clients.py`** (0065 is WS-5's; a WS-6 migration would be 0066). WS-6 reuses existing tables/workers end to end. **No migration is written.** |
> | **[TODO-3] `eval_passed` auto-gate** — "record it as a satisfied precondition… no build, just the honest ledger entry" (`eval_runner.py:309,326`) | **ALREADY SHIPPED *and* ALREADY RECORDED — zero work, task deleted.** The auto-set lives at **`eval_runner.py:531`** (agent) and **`:548`** (workflow) — the plan's `:309,326` is **drift**. More to the point, `docs/design/todo/execution-models-gap-analysis.md:98` **already reads `### TODO-3: … — ✅ RESOLVED`**. The plan's only TODO-3 deliverable was a ledger line that is already on the page. Nothing to do. |
> | **[TODO-7] Sandbox run TTL** — "the worker already sweeps **production** durable runs; WS-6 extends the same worker to also sweep **sandbox** runs" | **BACKWARDS — sandbox TTL is ALREADY SHIPPED; the plan's task is deleted.** `_sweep_stale_durable_runs()` (`approval_timeout_worker.py:166-192`) updates **`PlaygroundRun`** — the **sandbox/playground** run table — and filters on **`execution_shape`/`status`/`started_at` only, with NO environment or context predicate at all** (`:177-181`). It already sweeps every durable playground run past `_DURABLE_RUN_TTL_MINUTES=10` (`:33`). There is no production-only scoping to extend, and `PlaygroundRun` carries no `environment` column (it has `deployment_id` **and** `production_deployment_id`, `models.py:1337/1345`). Building `_sweep_once(scope)` would have **manufactured** a parameter for a distinction the table does not draw. |
> | (silent) | **THE REAL PARITY BUG — the plan never noticed it, and it is what WS-6 exists to fix.** `_agent_pod_url(agent_name, team, environment: str = "production")` (`approval_timeout_worker.py:36`) is **parameterized but never threaded**: **both** call sites take the default — `_notify_agent` (`:45`) and `routers/approvals.py:150`. Sandbox Services are named **`{agent}-sandbox`** (`deploy-controller/manifest_builder.py:21/56`, `f"{agent_name}-{environment}"`), so a **sandbox** approval's `/resume` POST is addressed to a **non-existent `-production` pod** — and the resulting `httpx.RequestError` is **swallowed to a `logger.warning`** (`:66-71`). Textbook "parallel paths drift, and safe defaults hide it". **The repo already knows**: `approvals.py:145-148` **works around it for the durable branch with an inline hand-built f-string** whose own comment names the bug — *"the -production default DNS-fails for a sandbox/playground agent (same wrong-target as the workflow path)"*. **One concern, two builders, one fixed and one not.** That is WS-6's core. |
> | (silent) | **`_team_namespace` has TWO definitions** — `workflow_orchestrator.py:49` and `routers/internal.py:44` — and **~8 hand-built agent-pod-URL f-strings** span 5 files (`workflow_orchestrator.py:77/136/194`, `internal.py:103/122`, `playground.py:345`, `approvals.py:148`, `approval_timeout_worker.py:40`). `internal.py:103/122` **hardcode `-production`** (deliberate per their comments — trigger runs target production; the fix makes that intent **explicit**, not inherited from a default). |
> | **[WS-0 OQ-10]** "fold `WORKFLOW_REACTIVE_TIMEOUT_S` into the shared timeout worker's scope parameterization so there is one run-timeout mechanism, not a constant + a worker" | **REJECTED — this would be a REGRESSION, not a parity fix. Ledger entry, not a task.** `WORKFLOW_REACTIVE_TIMEOUT_S` (`internal.py:289`) is an **in-request** `asyncio.wait_for(orchestrate(...), timeout=…)` that **holds the caller's HTTP connection** and calls `_fail_parent` on expiry (`:291-301`). `approval_timeout_worker` is a **60-second-poll background sweep** (`_POLL_INTERVAL_SECONDS = 60`, `:31`) of **abandoned rows**. A background poll **cannot** cap an in-request await — folding them would leave a reactive caller hanging **up to 60s past its own cap**. These are two legitimately different mechanisms that happen to share the word "timeout". Correctly separate; the plan's "one mechanism" instinct is right in general and wrong here. |
> | `studio/src/components/layout/*` (nav) | **DOES NOT EXIST.** There is no `studio/src/components/layout/` directory. The nav is **`studio/src/components/Sidebar.tsx`** — the **"Approvals" item already exists** (`:55`, `{ label: "Approvals", to: "/approvals", icon: ClipboardCheck }`) inside `CATALOG_ITEMS`, with active-state handling at `:97`. **No `Sidebar.test.tsx` exists** → T013 creates it. The badge (a pending **count**) is the only genuinely missing piece. |
> | `App.tsx:87` routes `/approvals` | **CORRECT** — `<Route path="/approvals" element={<ApprovalsInboxPage />} />` (`App.tsx:87`), imported at `:30`. The badge's destination already ships. |
> | `registryApi.ts` — add `getPendingApprovalsCount` | **NOT NEEDED — reuse, do not add.** `listPendingApprovals(team?, context?, reviewerScope?)` (`registryApi.ts:1501`) already returns `ApprovalInboxItem[]`; the badge count is `.length`. **No new endpoint, no new API method** — a `getPendingApprovalsCount` wrapper over the same GET would be a second path to one fact (and its own drift risk). The plan's own hedge ("reuse `listPendingApprovals` if a count endpoint is absent") is the right branch. |
> | **[TODO-4]** "the `Overview*` components already exist … and **`AgentDetailPage` uses them**; WS-6 makes CatalogDetailPage mount the same components" | **HALF WRONG — right fork, wrong counterpart.** `AgentDetailPage.tsx` has **ZERO** `Overview*` references (its tabs are `deployments`/`versions`/`settings`, `:31`, `:191-197`). The **sole** consumer of the shared set is **`DeploymentOverviewPage.tsx`** (imports `:14-17`, 4-way dispatch `:157-165`). The fork **is** real: `CatalogDetailPage.tsx` has an inline `OverviewTab` (`:205`, mounted `:165`) that re-derives `executionShape` from `config_snapshot` (`:236`) and hand-builds endpoints in a **3-branch** chain (`:245-259`). **And it has already drifted**: the fork handles `reactive`/`durable`/`scheduled` and has **NO event-driven branch**, while the shared set has **four** components. The drift the plan predicts in the abstract has **already happened in the concrete**. |
> | **[Browser Cache]** "`window.__STUDIO_BUILD` (`main.tsx:10`) exists; WS-6 makes the deploy fail-loud if a Studio image bump ships without updating the marker" | **PREMISE CONFIRMED, AND WORSE THAN STATED.** `main.tsx:10` reads `window.__STUDIO_BUILD = "0.1.76"` while `STUDIO_TAG="0.1.143"` — the marker is **67 versions stale**. And `grep -rn "__STUDIO_BUILD" studio/src studio/e2e scripts charts` returns **exactly one line: its own assignment**. **Nothing reads it.** It is a live **orphan** (DoD #3) that has silently lied for 67 tags. This is the `e3-never-ran-tag-not-bumped.md` class in its purest form. |
> | (silent) | **The inline sandbox/playground workflow approval slice is ALREADY SHIPPED and ALREADY FILED under a "WS-6" heading** — `docs/testing/manual-ui-e2e-test-plan.md:242`, *"Known gaps — WS-6 (operate parity: inline sandbox/playground workflow approval)"* (studio 0.1.130, frontend-only). WS-6's ledger **extends** that section; it must not duplicate or contradict it. |
>
> **Tags (read from `scripts/deploy-cpe2e.sh:269-277`, do not guess):** registry-api **`0.2.190`** (`:269`; `charts/agentshield/values.yaml:614`), studio **`0.1.143`** (`:272`; `values.yaml:933`). WS-6 bumps **both** → **`0.2.191`** / **`0.1.144`**. No sub-chart is involved (WS-4's event-gateway exception does not apply here).

> **WHAT WS-6 ACTUALLY BUILDS (after re-grounding).** Of the plan's six items: **TODO-3 = shipped + already recorded** (deleted), **TODO-7 = shipped, plan inverted** (deleted), **OQ-10 = rejected as a regression** (ledger). The three that survive — plus **one real bug the plan never saw** — are all the same shape: **one concern, N drifting copies**.
> 1. **Agent pod-URL resolution** — `environment` parameterized but never threaded; sandbox silently mis-addressed; one branch already patched inline (Phases 2–3, **MVP**).
> 2. **Overview shape dispatch** — shared components exist; `CatalogDetailPage` forked a copy that **already dropped event-driven** (Phase 4).
> 3. **Nav badge** — the item and the route both ship; only the count is missing (Phase 5).
> 4. **`__STUDIO_BUILD`** — an orphan marker, 67 tags stale, read by nothing (Phase 6).

> **NO-FAKES ACCEPTANCE (non-negotiable — CLAUDE.md "No Fakes in E2E").** The 7-defect durable-workflow bug (`docs/bugs/durable-workflow-live-path.md`) proved faked seams hide exactly the bugs living in them — six suites shipped green while the real path was broken. `suite-79` MUST create a **real** agent, **really deploy it to sandbox**, park a **real** approval from a **real** pod, drive expiry through the **real** `POST /approvals/{id}/reopen` API, let the **real** background worker sweep it, and assert **real committed** rows + the **real** worker's observable. **NO** monkeypatched `_sweep_once`/`_agent_pod_url`, **NO** mocked httpx, **NO** hand-crafted `approvals` rows with a back-dated `expires_at`, **NO** `page.route` stubs in Playwright. Model on **`scripts/e2e/suite-76-webhook-client-signing.sh`**. **Fail LOUDLY (not skip)** if a fixture is unreachable — an unprovable gate is never a pass.

> **CRASH-LOUD CENSUS (mandatory — the suite-74 lesson).** suite-74 once reported **"✅ PASSED"** while silently dropping 6 of 11 cases: the driver crashed mid-run, the bare `try/finally` wrote only the cases recorded before the crash, and `PASS>0 FAIL==0` read as green. suite-79 MUST carry **both** guards (T007): (a) a wrapping `except Exception` recording **`T-S79-999 driver ran every case without crashing`** as a **failure** + traceback, and (b) an **ID-based** completeness census **`T-S79-COMPLETE`** over a `REQUIRED_IDS` list (`suite-76:507-518` is the live idiom). A hardcoded case **count** was tried and immediately drifted — **IDs are the one source of truth**. Use **per-invocation `/tmp` paths** (`RUN_TAG="$(date +%s)$$"`, `suite-76:128-132`) and **write results BEFORE cleanup**.

> **PARITY GATE (CLAUDE.md "No Bandaid Fixes") — WS-6 *is* the parity slice, so this is the whole point, not a side constraint.** The recurring bug class is **two parallel paths drifting while a safe default hides it** (`docs/bugs/side-effecting-lost-on-declarative-runner-path.md`: `side_effecting` was threaded onto the SDK tool-builder but dropped by the declarative-runner's own builder — every tool silently mocked under record; it survived because the fail-closed default made it *safe* and a test against the *other* path passed). WS-6 has **two live instances of exactly that shape**: the `-production` default that DNS-fails quietly for sandbox, and the CatalogDetailPage overview fork that has already lost event-driven. **Every task here must collapse a path, never add one.** If a task tempts you toward a second component, a second worker, or an `if sandbox:` branch, that is the bug WS-6 exists to remove — stop and fix the design.

**MVP scope:** **Checkpoint 1** (Phases 2–3) — **one** env-aware agent pod-URL resolver with **no `production` default to fall into**, threaded at every operate call site, the inline duplicate at `approvals.py:148` **deleted**, and the whole thing proven by a **real** sandbox approval driven to a **real** timeout through the **real** worker, asserting the resume **reached the sandbox pod**. Target this first; the Studio work (CP2) is independent polish that rides on a correct backend.

---

## Phase 1 — Setup & Re-grounding
_Establish ground truth before writing code. No behavior change._

- [ ] [T001] Record the re-grounding table above into `ws6/plan.md` §status and **correct the four claims the plan gets wrong, in place**: (a) §1.2 + §6-T5 — TODO-3 is not just shipped, it is **already recorded** as `✅ RESOLVED` at `gap-analysis.md:98`; the plan's only TODO-3 deliverable is already on the page (**delete the task**); (b) §1.4 + §6-T2 — the worker already sweeps **sandbox** (`PlaygroundRun`, no environment predicate, `approval_timeout_worker.py:166-192`); the plan has it **backwards**, and the real bug is the **untraded `environment` param** at `:36/:45` + `approvals.py:150` (**replace the task**); (c) §1.6 + §6-T2 — **reject OQ-10**: an in-request `asyncio.wait_for` cap (`internal.py:289`) cannot be folded into a 60s background poll without a regression (record the reasoning, not a task); (d) §1.3 + §5 — the shared-Overview consumer is **`DeploymentOverviewPage.tsx:157-165`**, **not** `AgentDetailPage` (zero refs), and the nav lives in **`Sidebar.tsx:55`**, not a non-existent `components/layout/`. Also record: suite=**79**, **no migration**, tags **0.2.191**/**0.1.144**, and that `__STUDIO_BUILD` is an **orphan stale by 67 tags** — `docs/plan/execution-models-v2/ws6/plan.md`
  - **Verify:** `grep -n "suite-79\|0.2.191\|Sidebar.tsx\|DeploymentOverviewPage\|REJECTED" docs/plan/execution-models-v2/ws6/plan.md`

---

## Phase 2 — One agent pod-URL resolver (the parity core)
_Collapse `-production`-defaulting builders + the inline workaround onto ONE env-aware helper. Blocking prerequisite for CP1._

- [ ] [T002] Create **`services/registry-api/agent_endpoints.py`** — the single home for "where does this agent's pod live". Three symbols: (1) `def team_namespace(team: str) -> str` → `f"agents-{team}"` (**the one definition**; `workflow_orchestrator.py:49` and `internal.py:44` are duplicate defs of this today); (2) `def agent_pod_base(agent_name: str, team: str, environment: str) -> str` → `f"http://{agent_name}-{environment}.{team_namespace(team)}.svc.cluster.local:8080"` — **`environment` is REQUIRED and has NO default**; this is the load-bearing design choice, not a style preference: the entire live bug is a `= "production"` default that two call sites silently fell into (`approval_timeout_worker.py:36`), so the fix is to make forgetting-to-thread-it **structurally impossible** (a `TypeError` at import/callsite) rather than to guard it with a check (CLAUDE.md: *"prefer making illegal states unrepresentable over guarding against them"*); (3) `async def resolve_agent_pod_base(agent_name: str, team: str) -> str` — resolves the running deployment's environment (move the body of `workflow_orchestrator._resolve_agent_environment:53-67` verbatim: newest `running` `Deployment.environment` for the agent, `"production"` on miss/exception) then returns `agent_pod_base(..., environment=env)`. Docstring must state the invariant: **every** agent-pod URL in registry-api comes from here; a hand-built `svc.cluster.local:8080` f-string is a parity violation — `services/registry-api/agent_endpoints.py`
  - **Verify:** `python3 -c "import ast; ast.parse(open('services/registry-api/agent_endpoints.py').read())"` and `grep -c "def agent_pod_base\|def team_namespace\|def resolve_agent_pod_base" services/registry-api/agent_endpoints.py` → **3**
- [ ] [T003] Rewire the **worker** — this is the concretely-broken leg. In `approval_timeout_worker.py`: **delete** `_agent_pod_url` (`:36-40`, the `environment="production"` default that nothing threads) and its stale module docstring line promising `{agent_name}-production` (`:14`); make `_notify_agent` (`:43`) `await resolve_agent_pod_base(agent_name, team)` from T002, so a **sandbox** approval's `/resume` reaches **`{agent}-sandbox`** instead of a non-existent `-production` Service. **Also stop the failure being invisible:** the `except httpx.RequestError` at `:66-71` currently logs a bare `warning` — include the **resolved URL and environment** in the message and raise it to **`logger.error`**, because an unreachable resume pod is a real operational failure, and logging it as a shrug is precisely what let this survive (it must stay non-raising — a background worker that dies stops sweeping every other approval, so *loud* here means *loud in the log*, not a crash). Leave `_sweep_stale_durable_runs` (`:166-192`) **untouched** — it already sweeps sandbox correctly (see re-grounding) — `services/registry-api/approval_timeout_worker.py`
  - **Verify:** `python3 -c "import ast; ast.parse(open('services/registry-api/approval_timeout_worker.py').read())"`; `grep -c "_agent_pod_url" services/registry-api/approval_timeout_worker.py` → **0**; `grep -n "resolve_agent_pod_base" services/registry-api/approval_timeout_worker.py` → present
- [ ] [T004] Rewire **`routers/approvals.py`** and **delete the inline duplicate** — the payoff task. At `:143-150` there are today **two** builders behind an `if is_durable:`: an inline hand-built f-string using `_resolve_agent_environment` + `_team_namespace` (`:145-148`, the *fixed* copy, whose comment names the bug) and `_agent_pod_url(agent_name, team)` (`:150`, the *broken* copy that takes the `-production` default). **Both branches resolve the identical fact** — where the agent's pod is — so **collapse them to a single unconditional `pod_url = await resolve_agent_pod_base(agent_name, team)` computed BEFORE the `if`**, and drop the now-dead `from workflow_orchestrator import _resolve_agent_environment, _team_namespace` import (`:147`) plus the `from approval_timeout_worker import _agent_pod_url` import (`:26`). `is_durable` keeps deciding the **resume body** (`run_id`/`callback_url`/`eval_mode`, `:130-137`) — that is a genuine difference — but it must **not** decide the URL. Keep the explanatory comment, rewritten to record that the resolver is now shared. **Do not leave the ternary "just in case"**: two branches computing one fact is the drift engine — one already diverged from the other — `services/registry-api/routers/approvals.py`
  - **Verify:** `python3 -c "import ast; ast.parse(open('services/registry-api/routers/approvals.py').read())"`; `grep -c "_agent_pod_url\|_resolve_agent_environment" services/registry-api/routers/approvals.py` → **0**; `grep -c "svc.cluster.local:8080" services/registry-api/routers/approvals.py` → **0** (no hand-built URL remains)
- [ ] [T005] Converge the remaining duplicate **definitions** and make the two `-production` hardcodes **explicit** (bounded — do not refactor the whole dispatch layer): (a) `workflow_orchestrator.py` — delete `def _team_namespace` (`:49`) and `def _resolve_agent_environment` (`:53-67`), import both from `agent_endpoints`, keeping backwards-compatible module-level aliases **only if** `grep -rn "_resolve_agent_environment\|_team_namespace" services/registry-api` shows an external importer (`playground.py:345` and `approvals.py:147` import them today — `approvals.py`'s goes away in T004, so re-point `playground.py:345` at `resolve_agent_pod_base` and drop the aliases); its three member-dispatch f-strings (`:77/:136/:194`) already thread `{environment}` **correctly** — re-point them at `agent_pod_base(...)` so the string exists once; (b) `routers/internal.py` — delete `def _team_namespace` (`:44`) and call `agent_pod_base(agent_name, team, environment="production")` at `:103` and `:122`, replacing the hardcoded `-production` f-strings. **Keep production**: their comments state trigger runs deliberately target production — the change makes that intent an **explicit argument at the call site** instead of a silent inherited default, which is the entire point. **Out of scope (ledger, not this task):** `workflow_orchestrator.py:1061` (a *workflow* orchestrator pod, different naming convention) and `playground.py:538` (builds from `deployment.k8s_namespace`, not `team`) — `services/registry-api/workflow_orchestrator.py` + `services/registry-api/routers/internal.py` + `services/registry-api/routers/playground.py`
  - **Verify:** `python3 -c "import ast; [ast.parse(open(f).read()) for f in ['services/registry-api/workflow_orchestrator.py','services/registry-api/routers/internal.py','services/registry-api/routers/playground.py']]"`; `grep -rc "def _team_namespace" services/registry-api/ | grep -v ":0"` → only `agent_endpoints.py`

---

## Phase 3 — suite-79: real sandbox fixture, real timeout, crash-loud census, parity grep
_One file, appended sequentially (T006→T009). NO FAKES — real deploy, real pod, real worker, real rows._

- [ ] [T006] Scaffold **`suite-79-operate-parity.sh`** — `#!/usr/bin/env bash`, `set -euo pipefail`, `NAMESPACE`/`API_POD`/`ADMIN_SUB="75c7c8b3-7d2d-46e1-8a7b-938dd3c157c6"` (the real interactive platform-admin Keycloak sub — deny-by-default hides resources whose `created_by` ≠ the caller's sub), modeled on **`suite-76`**. **Per-invocation `/tmp` paths** (`suite-76:128-132`): `RUN_TAG="$(date +%s)$$"`, `DRIVER=/tmp/s79_driver_${RUN_TAG}.py`, `OUTFILE=/tmp/s79_out_${RUN_TAG}.txt`, `RUNLOG=/tmp/s79_run_${RUN_TAG}.log` — a fixed path lets two overlapping runs read each other's results. Build the **real** fixture: a real agent using **HTTP tools only** (python-type tools currently crash the pod — known open bug) with one **high-risk** tool so a real `/chat` **really parks** a real approval, **really deployed to `environment=sandbox`** (`POST /agents/{name}/deployments`) and waited to `running` — the sandbox deploy is the whole point, so **fail LOUDLY, never skip**, if the pod does not come up: a suite that skips its only sandbox fixture proves exactly nothing about the `-production` bug it exists to catch — `scripts/e2e/suite-79-operate-parity.sh`
  - **Verify:** `bash -n scripts/e2e/suite-79-operate-parity.sh`
- [ ] [T007] Add the **crash-loud + census guards** to suite-79 **before** any case (they are what make every later case trustworthy): (a) wrap the driver body in `except Exception` recording **`T-S79-999 driver ran every case without crashing`** as a **FAIL** + `traceback.format_exc()[-400:]`, re-raising into `finally` so cleanup still runs, and **write the result file BEFORE cleanup** (the suite-69 lesson) — mirror `suite-76:432`; (b) after the bash result parse, add the **ID-based** census `REQUIRED_IDS="000 001 002 003"` looping `grep -q "T-S79-$id"` → on any miss emit **`FAIL T-S79-COMPLETE every gate assertion ran | NEVER RAN:$MISSING`**, bump `FAIL`, and dump the driver-log tail; mirror `suite-76:501-518`. **Use IDs, never a hardcoded count** — a count reported GREEN on a half-run that silently dropped 6 of 11 cases and could not say *which* vanished. Exit non-zero on `FAIL≠0` **or** `PASS==0` (inconclusive) — `scripts/e2e/suite-79-operate-parity.sh`
  - **Verify:** `grep -n "T-S79-999\|T-S79-COMPLETE\|REQUIRED_IDS" scripts/e2e/suite-79-operate-parity.sh` shows all three
- [ ] [T008] Append **T-S79-000 parity grep** (the anti-drift assertion — run it first, it is cheap and catches the repo's #1 bug class): assert `grep -c "def agent_pod_base" services/registry-api/agent_endpoints.py` == **1**; `grep -rc "def _team_namespace" services/registry-api/` shows **exactly one file** (`agent_endpoints.py`) — the two duplicate defs are gone; `grep -c "_agent_pod_url" services/registry-api/approval_timeout_worker.py services/registry-api/routers/approvals.py` == **0** (the defaulting builder is deleted, not merely bypassed); `grep -c "svc.cluster.local:8080" services/registry-api/routers/approvals.py` == **0** (the inline duplicate is gone); and — the structural guard — `grep -c 'environment: str = "production"' services/registry-api/agent_endpoints.py` == **0** (**no default may reappear**; the default *is* the bug, and a future edit re-adding it would silently restore the exact failure this slice fixed). Any hand-built agent-pod f-string outside `agent_endpoints.py` is a parity violation — stop and fix the design, do not special-case — `scripts/e2e/suite-79-operate-parity.sh`
  - **Verify:** `bash scripts/e2e/suite-79-operate-parity.sh 2>&1 | grep "T-S79-000"`
- [ ] [T009] Append the **real sandbox resume + real TTL** cases — the no-fakes heart. **T-S79-001 (console decide → sandbox pod):** `/chat` the **real sandbox-deployed** agent so its **real pod** parks a **real** approval → `PATCH /approvals/{id}` `{"decision":"approved"}` (the **real** console path, the `else`-branch that took the `-production` default) → assert the resume **reached the sandbox pod**: the registry-api log carries `resume posted thread_id=… status=200` (`approvals.py:154`) and **zero** `failed to resume agent pod` for that `thread_id` (`:159`). **T-S79-002 (real worker, real expiry — no back-dated rows):** park a second real approval, then drive it to expiry **through real APIs only** — `PATCH /approvals/{id}` `{"decision":"rejected"}` → `POST /approvals/{id}/reopen` `{"timeout_seconds":1}` (`approvals.py:827`, which legitimately resets `status='pending'` + `expires_at = now+1s`, `:853-854`) → sleep past the worker's **60s** poll (`_POLL_INTERVAL_SECONDS`, `:31`) → assert the **real committed** row is `status='timed_out'` **and** the registry-api log shows `notified agent pod thread_id=… status=200` (`approval_timeout_worker.py:57-60`) with **no** `could not reach agent pod` (`:68`). **The log is the discriminator and the DB row is NOT** — `_cancel_runs_for_timed_out_approvals` (`:128-163`) marks the run `cancelled` **whether or not the pod was ever reached**, so asserting only the row would pass on the broken `-production` code and prove nothing; state this in a comment so nobody "simplifies" the assertion away. **T-S79-003 (production unbroken):** repeat T-S79-001 against a **production**-deployed agent → resume still reaches `{agent}-production` (the shared resolver must not regress the path that already worked) — `scripts/e2e/suite-79-operate-parity.sh`
  - **Verify:** `bash scripts/e2e/suite-79-operate-parity.sh 2>&1 | grep -E "T-S79-00[123]"`

---

## Checkpoint 1 — One env-aware pod-URL resolver, proven on a real sandbox timeout (**MVP GATE**)
_Gate: Phases 2–3 complete. Run before starting Phase 4._
_What you prove: one resolver with **no production default to fall into** serves every operate call site; a **real** sandbox approval decided through the **real** console reaches the **real** sandbox pod; a **real** approval driven to a **real** expiry is swept by the **real** worker and notifies the **real** sandbox pod; production is unregressed._

- [ ] [CP1a] Deploy script `scripts/deploy-cp1-ws6.sh` — thin idempotent wrapper: echo scope → **`bash scripts/deploy-cpe2e.sh`** → `kubectl rollout status deploy/agentshield-registry-api -n agentshield-platform --timeout=300s`. Builds **registry-api `0.2.191`** (T002–T005). The wrapper **delegates to `deploy-cpe2e.sh` and NEVER runs bare helm/docker/kubectl** (CLAUDE.md "Deploy Script Only" — a code edit that isn't built+deployed leaves the pod on OLD code). Requires T017's tag bumps to be committed first. **`rollout status` must complete before any suite runs** — testing seconds after a rollout reads the OLD pod and yields phantom failures — `scripts/deploy-cp1-ws6.sh`
  - **Verify:** `bash scripts/deploy-cp1-ws6.sh`
- [ ] [CP1b] Infra smoke `scripts/smoke-test-cp1-ws6-infra.sh` — REAL `kubectl`/`httpx` assertions, `exit 0` only on all-pass: **T-CP1B-001** registry-api pods `Running` on image tag **`0.2.191`** (`kubectl get pod -o jsonpath` over `.spec.containers[].image`), crashloop=0; **T-CP1B-002** alembic head is **still `0064`** — WS-6 adds **no** migration, so a moved head means someone wrote one that the re-grounding says is unnecessary; **T-CP1B-003** the **import gate** — `kubectl exec` into the registry-api pod and import the **real app** from a **writable** temp dir (`cd /tmp && PYTHONPATH=/app python3 -c "import agent_endpoints, approval_timeout_worker, main; print('ok')"`). **`ast.parse` is NOT enough** — T003/T004 delete imports and symbols across modules, and a missing import passes AST and then **CrashLoops the pod at startup**; this must run **on-cluster** (`/app` is read-only, so `cd /app` cannot write `__pycache__`, and the local py3.9 venv fails on PEP-604 annotations); **T-CP1B-004** the **structural guard survives deployment** — `kubectl exec … grep -c 'environment: str = "production"' /app/agent_endpoints.py` → **0**, and the served module has `def agent_pod_base` (proving the **deployed image**, not just the working tree, carries T002 — the `e3-never-ran-tag-not-bumped.md` lesson: a tag is a **claim about content**, and both tag files agreeing on a stale tag is exactly how that check stays green while the code never ran) — `scripts/smoke-test-cp1-ws6-infra.sh`
  - **Verify:** `bash scripts/smoke-test-cp1-ws6-infra.sh`
- [ ] [CP1c] Behaviour smoke `scripts/smoke-test-cp1-ws6-behaviour.sh` — runs **`bash scripts/e2e/suite-79-operate-parity.sh`** (T-S79-000..003 + T-S79-999 + T-S79-COMPLETE) and asserts all green: parity grep (1 `agent_pod_base` def, 1 `team_namespace` def, 0 `_agent_pod_url`, 0 inline URLs, **0 production defaults**); real sandbox console decide → resume reaches the **sandbox** pod; real reopen-driven expiry → real worker → `timed_out` + pod notified; production unregressed. Explicit `jq`/HTTP-status checks; `exit 0` only on all-pass. **This IS the no-fakes gate** — `scripts/smoke-test-cp1-ws6-behaviour.sh`
  - **Verify:** `bash scripts/smoke-test-cp1-ws6-behaviour.sh`

> **To run:** `bash scripts/deploy-cp1-ws6.sh` → wait for `kubectl rollout status` → `bash scripts/smoke-test-cp1-ws6-infra.sh && bash scripts/smoke-test-cp1-ws6-behaviour.sh`
> **Pass criteria:** all assertions exit 0; no pod in CrashLoopBackOff; alembic head **still `0064`**; `agent_pod_base` = 1 def with **no** `production` default; `_agent_pod_url` = 0 references; the sandbox resume reaches `{agent}-sandbox`; `T-S79-COMPLETE` confirms every case reported.

---

## Phase 4 — Overview shape dispatch: one explicit map, two mount points
_Collapse the CatalogDetailPage fork onto the shared components — via a shared **dispatcher**, not a second hand-written ternary chain._

- [ ] [T010] Create **`studio/src/components/agent-detail/OverviewForShape.tsx`** — the one place that answers "which Overview does this shape get". The live dispatch at `DeploymentOverviewPage.tsx:157-165` is a **ternary fallthrough chain** (`hasWebhook ? … : hasSchedule ? … : shape === "durable" ? … : Reactive`), and `CatalogDetailPage.tsx:245-259` is a **hand-written second copy that already dropped the event-driven case entirely**. Ship an **explicit map**, never a priority chain: `const OVERVIEW_BY_SHAPE: Record<ExecutionShape, ComponentType<OverviewProps>> = { reactive: OverviewReactive, durable: OverviewDurable, scheduled: OverviewScheduled, event_driven: OverviewEventDriven }` plus `export default function OverviewForShape({ shape, agentName, deploymentId, context }: Props)` that looks the shape up and — on an **unhandled** shape — **fails closed and loud** (render a visible "unsupported execution shape: X" error card **and** `console.error`), never a silent fallback to Reactive: a quiet default is precisely how the fork lost event-driven without anyone noticing. Reuse the existing `OverviewProps` contract verbatim (`agentName`, `deploymentId`, `context: DeploymentContext` — `OverviewReactive.tsx:10-16`, `registryApi.ts:338`); add `export type ExecutionShape = "reactive" | "durable" | "scheduled" | "event_driven"` so a new shape is a **compile error at the map**, not a runtime surprise — `studio/src/components/agent-detail/OverviewForShape.tsx`
  - **Verify:** `cd studio && npm run typecheck` and `grep -n "OVERVIEW_BY_SHAPE\|console.error" studio/src/components/agent-detail/OverviewForShape.tsx`
- [ ] [T011] Wire **both** mount points to the one dispatcher — the parity payoff. (a) `DeploymentOverviewPage.tsx` — replace the ternary chain (`:157-165`) with a single `<OverviewForShape shape={…} agentName={name!} deploymentId={deployment.id} context={context} />`, deriving `shape` from the existing `hasWebhook`/`hasSchedule`/`agent?.execution_shape` signals **in one place** (webhook ⇒ `event_driven`, schedule ⇒ `scheduled`, else the agent's `execution_shape`); drop the four now-unused direct imports (`:14-17`). (b) `CatalogDetailPage.tsx` — **delete the inline `OverviewTab` overview markup**: the hand-derived `executionShape` (`:236`), the 3-branch `endpoints` builder (`:244-259`), and the endpoint/health markup it feeds; mount `<OverviewForShape shape={executionShape} agentName={artifact.name} deploymentId={deployment.id} context="production" />` instead (catalog artifacts are published/production by definition — pass it **explicitly**, never by default). **Keep** the catalog-only concerns the shared components do not own — the `StatCard` metrics row (`:255-285`), deployment actions (`onAction`/`onDeploy`), member topology, versions. **This restores event-driven to the catalog for free** — the fork's missing 4th branch — which is exactly the drift the shared dispatcher exists to make impossible — `studio/src/pages/DeploymentOverviewPage.tsx` + `studio/src/pages/CatalogDetailPage.tsx`
  - **Verify:** `cd studio && npm run typecheck`; `grep -c "OverviewForShape" studio/src/pages/DeploymentOverviewPage.tsx studio/src/pages/CatalogDetailPage.tsx` → **1 each**; `grep -c "OverviewReactive\|OverviewDurable\|OverviewScheduled\|OverviewEventDriven" studio/src/pages/CatalogDetailPage.tsx studio/src/pages/DeploymentOverviewPage.tsx` → **0 each** (no direct mounts survive the dispatcher)
- [ ] [T012] [P] Vitest for the dispatcher + both mount points (mock via `vi.mock('../../api/registryApi')`, render via `renderWithProviders` from `src/test/utils.tsx`): `OverviewForShape.test.tsx` — **all four** shapes render their component (**including `event_driven`, the case the fork silently lacked**), and an **unknown** shape renders the loud error card + calls `console.error` (**not** a silent Reactive fallback — assert the fail-closed behaviour explicitly, or the next refactor will quietly re-add the default); `CatalogDetailPage.test.tsx` (**new file** — none exists) — the overview tab mounts `OverviewForShape` with `context="production"`, and a **webhook/event-driven** artifact renders the event-driven overview (**the regression test for the exact drift being fixed**); extend `DeploymentOverviewPage.test.tsx` (exists; note its `:79` comment already asserts *"OverviewReactive renders the API Endpoint card"*) so its existing shape expectations hold through the dispatcher — `studio/src/components/agent-detail/OverviewForShape.test.tsx` + `studio/src/pages/CatalogDetailPage.test.tsx` + `studio/src/pages/DeploymentOverviewPage.test.tsx`
  - **Verify:** `cd studio && npm run test`
- [ ] [T013] [P] Playwright **`catalog-overview-parity.spec.ts`** — real Keycloak login (`e2e/global-setup.ts`), against the **https gateway `https://agentshield.127.0.0.1.nip.io:8443`** (Secure Keycloak cookies break over an http port-forward). **NO `page.route` stubs.** **CREATE the fixture, never scavenge** "the first catalog row" — a scavenging spec's verdict tracks leftover cluster state and dies on a fixture that structurally cannot satisfy it; publish a real artifact whose shape the test needs. Drive: open a real catalog artifact → Overview tab → assert the **shared** overview rendered (a `data-testid` owned by the shared component, so the assertion **cannot** pass against the old inline fork) → assert the same testid renders on **`/agents/:name/d/:depId`** (`DeploymentOverviewPage`, `App.tsx:63`) for the same shape — **that cross-page identity IS the parity proof**. **Re-open the tab after any reload** — `CatalogDetailPage`/`AgentDetailPage` keep `activeTab` in **local state** (`AgentDetailPage.tsx:31` defaults to `"deployments"`), so a reload silently lands on the wrong tab and the assertion fails for a reason that has nothing to do with the code under test. Note `e2e/*.spec.ts` is **NOT typechecked** (`studio/tsconfig.json` includes only `"src"`), so a typo here passes `npm run typecheck` and dies at runtime — run the spec — `studio/e2e/catalog-overview-parity.spec.ts`
  - **Verify:** `bash scripts/studio-e2e.sh`

---

## Phase 5 — Global Approvals Inbox nav badge
_The item and the route already ship; only the count is missing. Reuse the existing producer — add no endpoint._

- [ ] [T014] Add the pending-count badge to **`Sidebar.tsx`** — the nav item already exists (`:55`, `{ label: "Approvals", to: "/approvals", icon: ClipboardCheck }` in `CATALOG_ITEMS`) and `/approvals` already routes to `ApprovalsInboxPage` (`App.tsx:87`), so this is **only** the count. Add an optional `badge?: number` to the `NavItem` shape and render it as a pill on the item when `> 0` (hidden at 0 — a "0" badge is noise, and an always-on chip trains operators to ignore it). Source the count from the **existing** `listPendingApprovals()` (`registryApi.ts:1501`) via React Query with a sane `refetchInterval` — **do NOT add `getPendingApprovalsCount`**: a second API method over the same `GET /approvals/` would be a second path to one fact, and this slice exists to delete those (it would also be an orphan the moment the badge changed shape). Give the pill a stable `data-testid="approvals-badge"` — the Playwright spec and the **served-bundle** grep (T016) both key on it — `studio/src/components/Sidebar.tsx`
  - **Verify:** `cd studio && npm run typecheck` and `grep -n "approvals-badge\|listPendingApprovals" studio/src/components/Sidebar.tsx`
- [ ] [T015] [P] Vitest **`Sidebar.test.tsx`** (**new file** — no Sidebar test exists today): with `listPendingApprovals` mocked to N pending items the badge renders **N**; with **0** items **no badge renders** (assert absent, not `"0"`); the badge is inside the link to `/approvals` (so clicking it routes — the plan's acceptance is "shows the count **and routes**", and a count that doesn't navigate is half a feature); a **failing** `listPendingApprovals` renders the nav item **without** a badge and does not crash the whole sidebar (an approvals outage must not take out navigation) — `studio/src/components/Sidebar.test.tsx`
  - **Verify:** `cd studio && npm run test`
- [ ] [T016] [P] Playwright **`approvals-badge.spec.ts`** — real Keycloak login, **https gateway**, **no `page.route` stubs**. **CREATE the fixture** (a real agent → real high-risk `/chat` → a **real** parked approval), never assert against whatever pending rows the cluster happens to hold — a scavenged count is a verdict on leftover state. Drive: load Studio → `page.waitForResponse` on `GET **/approvals/**` → assert `[data-testid="approvals-badge"]` shows a count ≥ 1 → **click the badge** → assert the URL is `/approvals` and the inbox page rendered (the badge's whole contract is count **+** route). Then **decide the approval through the real console** (`PATCH /approvals/{id}`) → **reload** → assert the badge count **decremented** — the save→reload→assert round-trip (DoD #2) proving the badge reads live backend state rather than a cached mount-time number — `studio/e2e/approvals-badge.spec.ts`
  - **Verify:** `bash scripts/studio-e2e.sh`

---

## Phase 6 — `__STUDIO_BUILD`: make the marker true, and make a lie fail the deploy
_The marker is an orphan, 67 tags stale, read by nothing. Give it a reader and an enforcer._

- [ ] [T017] Fix the marker and **give it a reader** — `main.tsx:10` currently sets `window.__STUDIO_BUILD = "0.1.76"` while `STUDIO_TAG="0.1.143"`, and `grep -rn "__STUDIO_BUILD" studio/src studio/e2e scripts charts` returns **exactly one line — its own assignment**. A value nothing reads cannot fail loudly, which is why it drifted 67 tags unnoticed (DoD #3: a new symbol with no live caller is orphan debt; this one has been debt since 0.1.76). (a) Update it to **`"0.1.144"`** (this slice's tag); (b) **stop it being hand-maintained** — source it from the build (`import.meta.env.VITE_STUDIO_BUILD ?? "dev"`, with the Vite define/env supplied at image build) so the marker is a **fact about the bundle** rather than a promise someone remembered to update; (c) give it the reader that makes a stale bundle **visible**: render it into a `data-testid="studio-build"` element (a small footer/`title` attribute in the sidebar is enough) so a human and a Playwright spec can both see which bundle is actually being served. **This is the `e3-never-ran-tag-not-bumped.md` class**: both tag files agreed on a stale tag and the cluster faithfully matched it, so every existing check stayed green while the code never ran — the only durable fix is an assertion against **served content** (T018/CP2b), and that needs a reader — `studio/src/main.tsx` + `studio/src/components/Sidebar.tsx`
  - **Verify:** `cd studio && npm run typecheck` and `grep -rn "__STUDIO_BUILD\|VITE_STUDIO_BUILD" studio/src` shows **both** a writer **and** a reader
- [ ] [T018] Make the deploy **fail loud** on a marker/tag mismatch — `deploy-cpe2e.sh` must abort **before** pushing a Studio image whose build marker disagrees with `STUDIO_TAG` (`:272`). Add a guarded check in the Studio build section: resolve the marker the bundle will carry (the `VITE_STUDIO_BUILD` value / `main.tsx` literal), compare to `$STUDIO_TAG`, and on mismatch `echo` the two values and **`exit 1`** with a message naming the failure mode ("the served bundle would claim X while the image is tagged Y — a stale bundle serves silently"). **Fail closed:** if the marker cannot be resolved at all, **abort** rather than proceed — an unverifiable claim is not a pass. Pair it with the coupling check the E-3 bug already institutionalised: `scripts/smoke-test-cp1-e3-constitution.sh` (`AUDIT_REF`, `:230-247`) asserts *"code changed under `studio/` ⇒ `STUDIO_TAG` was bumped in this commit"* — that catches the **tag**, this catches the **content**, and E-3 proved you need both (its `deploy-cpe2e.sh` and `values.yaml` **agreed**… on the stale tag) — `scripts/deploy-cpe2e.sh`
  - **Verify:** `bash -n scripts/deploy-cpe2e.sh`; with a deliberately-stale marker the Studio build step **aborts non-zero**; with the matching `0.1.144` marker it proceeds

---

## Phase 7 — Post-impl gates (register suite, bump tags, docs, gap ledger, orphan sweep)
_Five independent file groups → parallelizable. Closes the CLAUDE.md Post-Implementation Checklist._

- [ ] [T019] [P] Bump **both** tag files in the same change — `scripts/deploy-cpe2e.sh`: `REGISTRY_API_TAG` `0.2.190→0.2.191` (`:269`), `STUDIO_TAG` `0.1.143→0.1.144` (`:272`), plus a WS-6 comment-header entry (`:220+` idiom); `charts/agentshield/values.yaml`: registry-api tag `:614` → `0.2.191`, studio tag `:933` → `0.1.144`. The deploy uses `helm upgrade` with tags baked into values (no `--set`), so bumping only the script leaves the chart pointing at the old tag. **No sub-chart here** (WS-4's event-gateway exception does not apply). **Bumping both files is necessary but NOT sufficient** — E-3's two files agreed on a stale tag while the cluster faithfully served old code; the content assertions (T018, CP1b T-CP1B-004, CP2b T-CP2B-002) are what actually prove the image changed — `scripts/deploy-cpe2e.sh` + `charts/agentshield/values.yaml`
  - **Verify:** `grep -n "0.2.191\|0.1.144" scripts/deploy-cpe2e.sh charts/agentshield/values.yaml` shows **both** tags in **both** files
- [ ] [T020] [P] Register suite-79 in the runner **after suite-77** (`run-all.sh:126`) — add `run_suite "Suite 79: WS-6 operate parity — one pod-URL resolver (no-fakes)" "suite-79-operate-parity.sh"` and include it in the run loop / totals. **Do not take 78** — WS-5 is claiming it concurrently; if suite-78 is absent when you land, leave the gap rather than renumbering — `scripts/e2e/run-all.sh`
  - **Verify:** `grep -n "suite-79" scripts/e2e/run-all.sh`
- [ ] [T021] [P] **Reconcile `execution-models-gap-analysis.md` against the code** (CLAUDE.md DoD #6 — the doc claims more remains than the code shows, and this task is where that gets corrected rather than perpetuated): **TODO-1** (`:76`) → resolved in WS-6 (badge shipped; note the count reuses `listPendingApprovals`, no new endpoint); **TODO-3** (`:98`) → **already `✅ RESOLVED`; only fix the stale citation** `eval_runner.py:309,326` → **`:531`/`:548`**; **TODO-4** (`:104`) → resolved in WS-6, **and correct the record**: the fork's counterpart was `DeploymentOverviewPage`, not `AgentDetailPage`, and the fork had **already lost event-driven** before anyone collapsed it; **TODO-7** (`:163`) → **correct the claim outright** — sandbox TTL was **already shipped** (`_sweep_stale_durable_runs` sweeps `PlaygroundRun` unconditionally); the real bug was the **untraded `environment` param**, fixed here; **Browser Cache** (`:169`) → resolved (marker sourced from the build + read + deploy-enforced). **Keep** TODO-2 (shipped, WS-3), TODO-5, TODO-6 with cross-refs. Update the Build Priority Order block (`:177`) — `docs/design/todo/execution-models-gap-analysis.md`
  - **Verify:** `grep -n "WS-6\|RESOLVED\|531" docs/design/todo/execution-models-gap-analysis.md`
- [ ] [T022] [P] Record the **WS-6 gap ledger** in the canonical place — **extend the EXISTING `## Known gaps — WS-6` section at `docs/testing/manual-ui-e2e-test-plan.md:242`** (which already covers the shipped inline sandbox/playground workflow approval, studio 0.1.130); **do not open a second WS-6 heading**. Add the ledger table below, tagged **deferred (intentional)** vs **not-yet-wired (debt)** vs **rejected (with reasoning)**. Note the **`docs/experience/playground.md` decision**: WS-6 changes no playground SSE event, endpoint, panel, or routing rule — the pod-URL fix is invisible to the playground contract and the badge/Overview work is outside its covered surface — so **no update is required**; record that judgement here rather than leaving it as an unexplained skip of a CLAUDE.md §3 covered-file check. Never let an unfinished piece read as shipped (DoD #5) — `docs/testing/manual-ui-e2e-test-plan.md`
  - **Verify:** `grep -c "^## Known gaps — WS-6" docs/testing/manual-ui-e2e-test-plan.md` → **1** (extended, not duplicated)
- [ ] [T023] Orphan-grep + verification sweep — for **every** new symbol confirm a live caller/reader (DoD #3; "build utility + not wiring it up = NOT done"), and note that **WS-6 begins with a live orphan of exactly this kind** (`__STUDIO_BUILD`, unread for 67 tags), so this sweep is the slice's own lesson applied to itself: `grep -rn "agent_pod_base\|team_namespace\|resolve_agent_pod_base" services/registry-api` (readers = the worker, `approvals.py`, `internal.py`, `workflow_orchestrator.py`, `playground.py`); `grep -rn "OverviewForShape" studio/src` (readers = **both** `DeploymentOverviewPage` and `CatalogDetailPage` — one mount point alone means the fork was moved, not collapsed); `grep -rn "approvals-badge\|studio-build" studio/src` (each rendered **and** asserted). Confirm the **deletions** too — `_agent_pod_url`, the duplicate `_team_namespace` defs, and `CatalogDetailPage`'s inline endpoints chain must return **0**; a helper that survives beside its replacement is a fork with extra steps. Run the full set: `python3 -c "import ast; ast.parse(...)"` over every touched `.py` **plus the on-cluster real-app import** (CP1b T-CP1B-003 — `ast.parse` cannot catch the missing imports these deletions risk); `cd studio && npm run typecheck && npm run test` (**277 green today — keep them green**). Record results in — `docs/plan/execution-models-v2/ws6/plan.md`
  - **Verify:** `cd studio && npm run typecheck && npm run test` and the grep set above returns a caller for **every** new symbol and **0** for every deleted one

---

## Checkpoint 2 — Overview parity + badge + build marker + full suite-79 green
_Gate: Phases 4–7 complete. Final WS-6 gate._
_What you prove: one dispatcher serves the catalog and deployment overviews (event-driven restored), the nav badge counts and routes and survives a reload, the served bundle proves it is the new bundle, and suite-79 is green and registered._

- [ ] [CP2a] Deploy script `scripts/deploy-cp2-ws6.sh` — thin wrapper: echo scope → **`bash scripts/deploy-cpe2e.sh`** (builds + deploys **studio `0.1.144`** for T010–T017, alongside the already-rolled registry-api `0.2.191`) → `kubectl rollout status deploy/agentshield-studio -n agentshield-platform --timeout=300s`. **Delegates to `deploy-cpe2e.sh`; never bare helm/docker/kubectl.** **Wait for `rollout status` to return before running any spec** — Playwright against a mid-rollout Studio yields phantom failures — `scripts/deploy-cp2-ws6.sh`
  - **Verify:** `bash scripts/deploy-cp2-ws6.sh`
- [ ] [CP2b] Infra smoke `scripts/smoke-test-cp2-ws6-infra.sh` — REAL assertions, `exit 0` only on all-pass: **T-CP2B-001** studio pod `Running` on image tag **`0.1.144`** (`kubectl get pod -o jsonpath`), crashloop=0; **T-CP2B-002** **the served BUNDLE contains this slice's code** — fetch the Studio assets through the real edge and `grep` the served JS for **`approvals-badge`** (T014's testid) and the **`0.1.144`** build marker. **This is the assertion the E-3 bug existed for**: a tag is a **claim about content**, and E-3's studio bundle shipped with *"`scheduled-job-spec`: 0 occurrences"* while both tag files agreed and every other check stayed green. A pod running "0.1.144" proves the **label**; only a testid in the **served bytes** proves the **code**; **T-CP2B-003** `AUDIT_REF=HEAD bash scripts/smoke-test-cp1-e3-constitution.sh` green — the service-dir ⇄ tag coupling check (`:230-247`) confirms `studio/` and `services/registry-api/` changes each carry a tag bump **in this commit**; **T-CP2B-004** suite-79 registered (`grep -n "suite-79" scripts/e2e/run-all.sh`) and **both** tags present in **both** files (`0.2.191` + `0.1.144` in `deploy-cpe2e.sh` and `charts/agentshield/values.yaml`) — `scripts/smoke-test-cp2-ws6-infra.sh`
  - **Verify:** `bash scripts/smoke-test-cp2-ws6-infra.sh`
- [ ] [CP2c] Behaviour smoke `scripts/smoke-test-cp2-ws6-behaviour.sh` — `cd studio && npm run typecheck && npm run test` (Vitest incl. the new `OverviewForShape.test.tsx`, `CatalogDetailPage.test.tsx`, `Sidebar.test.tsx`; **277 existing tests stay green — do not delete or skip one to make a change pass**); **`bash scripts/studio-e2e.sh`** runs `catalog-overview-parity.spec.ts` (shared overview on **both** pages) + `approvals-badge.spec.ts` (count → click → `/approvals` → decide → **reload → decremented**) green against the **https gateway**; then **`bash scripts/e2e/suite-79-operate-parity.sh`** **fully green** (T-S79-000..003 + `T-S79-COMPLETE`). `exit 0` only on all-pass — `scripts/smoke-test-cp2-ws6-behaviour.sh`
  - **Verify:** `bash scripts/smoke-test-cp2-ws6-behaviour.sh`

> **To run:** `bash scripts/deploy-cp2-ws6.sh` → wait for `kubectl rollout status` → `bash scripts/smoke-test-cp2-ws6-infra.sh && bash scripts/smoke-test-cp2-ws6-behaviour.sh` → `bash scripts/studio-e2e.sh` (Playwright, separate gate)
> **Pass criteria:** studio on `0.1.144` **and the served bundle greps clean** for `approvals-badge` + `0.1.144`; `OverviewForShape` mounted on **both** pages with **0** direct Overview mounts left; suite-79 all-green + `T-S79-COMPLETE` + registered; typecheck + Vitest (277+) + Playwright green; E-3 constitution check green on `HEAD`.

---

## Summary Table

| Phase | Type | Tasks | Proves / Delivers |
|---|---|---|---|
| 1 — Setup & Re-grounding | impl | T001 | Ground truth recorded; the plan's two shipped items deleted, OQ-10 rejected with reasoning, the real pod-URL bug documented |
| 2 — One pod-URL resolver | impl | T002–T005 | `agent_endpoints.py`: 1 `team_namespace`, 1 `agent_pod_base` with **no production default**, 1 async resolver; worker + `approvals.py` inline duplicate collapsed; `-production` hardcodes made explicit |
| 3 — suite-79 (no-fakes) | impl | T006–T009 | Crash-loud + ID census; parity grep incl. the **no-default** guard; real sandbox console decide → sandbox pod; real reopen-driven expiry → real worker; production unregressed |
| **Checkpoint 1 (MVP)** | gate | CP1a, CP1b, CP1c | Deploy (api `0.2.191`) + infra (head **still 0064**, on-cluster real-app import, deployed-module guard) + the no-fakes behaviour gate |
| 4 — Overview dispatch | impl | T010–T013 | `OverviewForShape` explicit map (fail-closed on unknown shape); CatalogDetailPage fork deleted, **event-driven restored**; Vitest + Playwright cross-page identity |
| 5 — Nav badge | impl | T014–T016 | `Sidebar` pending-count pill reusing `listPendingApprovals` (**no new endpoint**); Vitest incl. 0-state + API-failure; Playwright count → route → reload-decremented |
| 6 — Build marker | impl | T017, T018 | `__STUDIO_BUILD` sourced from the build + **given a reader**; `deploy-cpe2e.sh` aborts on a marker/tag mismatch |
| 7 — Post-impl gates | impl | T019–T023 | Two-file tag bump; suite-79 registered; gap-analysis reconciled to the code; ledger extended in place; orphan **and deletion** sweep |
| **Checkpoint 2** | gate | CP2a, CP2b, CP2c | Studio `0.1.144` + **served-bundle** content proof + E-3 constitution + full suite-79 + typecheck/Vitest/Playwright |

**Parallel opportunities:** T012 `[P]` + T013 `[P]` (test/spec files, distinct from the `.tsx` edits); T015 `[P]` + T016 `[P]`; T019–T022 `[P]` (four independent file groups). Same-file appends to `suite-79` (T006→T007→T008→T009) are **sequential** (one file). Phase 2 → 3 is a hard chain (resolver → the suite that proves it); **T002 must land before T003/T004/T005** or the imports dangle and the pod CrashLoops at startup. Phases 4/5/6 are mutually independent and may land in any order after CP1.

---

## Gap Ledger (carried from plan §7, **re-grounded** against live code)

| Item | Status | Note |
|---|---|---|
| **[OQ-10] Fold `WORKFLOW_REACTIVE_TIMEOUT_S` into the timeout worker** | **REJECTED (design, not debt)** | **The plan's task would be a regression.** `internal.py:289` is an **in-request** `asyncio.wait_for` holding the caller's connection, failing the parent at the cap; `approval_timeout_worker` is a **60s-poll background sweep** (`:31`) of abandoned rows. A background poll cannot cap an in-request await — folding them leaves a reactive caller hanging **up to 60s past its cap**. Two mechanisms sharing a word, not a fork. **They stay separate by design.** The residue is cosmetic: the reactive cap is an `os.getenv` default (120s) and the TTL a module constant (`_DURABLE_RUN_TTL_MINUTES=10`) — two config styles, one config concern. Not worth a slice. |
| **[TODO-7] Sandbox durable-run TTL** | **ALREADY SHIPPED (plan inverted it)** | `_sweep_stale_durable_runs` (`approval_timeout_worker.py:166-192`) sweeps **`PlaygroundRun`** — the sandbox/playground table — with **no** environment/context predicate. The plan's "extends the worker to also sweep sandbox" describes work that does not exist. **No task.** What *was* broken (the untraded `environment` on the resume URL) is fixed in Phase 2. |
| **[TODO-3] `eval_passed` auto-gate** | **ALREADY SHIPPED *and* ALREADY RECORDED** | `eval_runner.py:531` (agent) / `:548` (workflow); `gap-analysis.md:98` already reads `✅ RESOLVED`. The plan's only deliverable was a ledger line already on the page. **No task** — T021 fixes the stale `:309,326` citation, nothing more. |
| **[TODO-2] Alerting** | **shipped (WS-3)** | Email alerting via the shared `alerting.dispatch_failure_alert` seam (`internal.py::_dispatch_and_complete`), proven by suite-71 T-S71-005. Slack/PagerDuty = future transports on the same seam. Stays in gap-analysis. |
| **[TODO-5] Role-based run/memory filtering** | **stays in gap-analysis (cross-referenced)** | The `agent:reviewer` **producer** landed in WS-1/WS-2; broad user-scoping of runs + memory is a separate gap-analysis item, **not absorbed by WS-6**. |
| **[TODO-6] Redis memory hot path** | **stays in gap-analysis** | Performance; deferred until latency matters. |
| **Approval-TTL timing is driven via `reopen`, not a 30-min wall clock** | **deferred (intentional) — accepted test design** | A real pod mints approvals at `APPROVAL_TTL_MINUTES=30` (`sdk/agentshield_sdk/hitl.py:31`); waiting that out is not viable in a suite, and back-dating `expires_at` in SQL would be a **hand-crafted row** (forbidden). T-S79-002 instead drives expiry through the **real** `POST /approvals/{id}/reopen` API (`approvals.py:827`) with `timeout_seconds=1` — every step a real API call against a real row. The **sweep + notify** path is therefore fully real; only the *route to a short expiry* differs from a natural 30-minute wait. |
| **`workflow_orchestrator.py:1061` + `playground.py:538` pod URLs not converged** | **not-yet-wired (debt, intentional)** | `:1061` targets a **workflow orchestrator** pod (`{wf}-production`, different naming convention); `:538` builds from `deployment.k8s_namespace` rather than `team`. Neither is the *agent*-pod concern `agent_endpoints.py` owns, and folding them in would mean widening the helper to two conventions — the fork-by-generalisation trap. Convergence deferred until a workflow-pod parity slice needs it. |
| **`list_approvals` authority-scoping is not context-discriminated** | **latent (by-design, dormant)** | Pre-existing; documented at `manual-ui-e2e-test-plan.md:242+`. `decide_approval` gates reviewer authority on **production only**, while `list_approvals` applies its `X-User-Sub` filter for every context. Dormant: Studio sends a `Bearer` JWT and **no** `X-User-Sub`. The badge (T014) reads the same endpoint and does **not** change this. Unchanged by WS-6; carried so it isn't a surprise. |

**No orphan flags** — WS-6 wires readers to producers that already exist and **deletes** the paths it replaces: `agent_endpoints` (readers = worker, `approvals.py`, `internal.py`, `workflow_orchestrator.py`, `playground.py`; the replaced `_agent_pod_url` + duplicate `_team_namespace` defs are **removed**, not left beside it), `OverviewForShape` (readers = **both** detail pages; the inline fork is **deleted**), the badge (producer = the existing `listPendingApprovals`; **no new endpoint**), `__STUDIO_BUILD` (**gains its first reader in 67 tags**). T023 greps a live caller for every new symbol **and 0 for every deleted one** before WS-6 is reported done.

---

## Definition of Done (WS-6)

- **Real user journey proven (DoD #1):** `studio/e2e/approvals-badge.spec.ts` (T016) drives count → click → `/approvals` → decide → reload in a real browser against the https gateway; `catalog-overview-parity.spec.ts` (T013) proves the **same** shared overview renders on both the catalog and deployment pages. **No `page.route` stubs**; both **create** their fixtures rather than scavenging cluster state. suite-79 drives the **real** worker and **real** sandbox pod, not a mock.
- **Save → reload → assert (DoD #2):** T016 decides an approval, **reloads from the backend**, and asserts the badge count decremented (proving it reads live state, not a mount-time cache). WS-6 adds **no new write surface** — the pod-URL fix, the Overview dispatcher, and the badge are all read paths — so no other round-trip guard is owed; T-S79-002 nonetheless re-reads the swept `approvals` row **from the DB**, never from a response.
- **No orphans (DoD #3):** T023 greps a live caller for `agent_pod_base`/`team_namespace`/`resolve_agent_pod_base`, `OverviewForShape` (**both** mount points), `approvals-badge`, `studio-build` — **and 0 references** for the deleted `_agent_pod_url`, the duplicate `_team_namespace` defs, and the inline endpoints chain. `__STUDIO_BUILD` was itself a live orphan for 67 tags; T017 gives it a reader and T018/CP2b make it fail loud.
- **Vertical slice (DoD #4):** the pod-URL resolver is proven end-to-end at **CP1** — helper → worker + console decide → real sandbox pod → real committed row — **before** any Studio work starts. The Studio phases (4/5/6) are independent and land after.
- **Honest gap ledger (DoD #5):** above — OQ-10 **rejected** with reasoning; TODO-3/TODO-7 **already shipped** (the plan overstated both); the `reopen`-driven expiry and the two unconverged workflow/playground URLs recorded as **deferred (intentional)**. Extended **in place** at `manual-ui-e2e-test-plan.md:242` (T022), not duplicated.
- **Reason from the running product (DoD #6):** every plan specific was re-verified against live code — suite 61→**79**; **no migration** (the plan's one clean call, head still `0064`); TODO-3 shipped **and already recorded**; TODO-7 **backwards**; OQ-10 a **regression if built**; the shared-Overview consumer is **`DeploymentOverviewPage`**, not `AgentDetailPage` (zero refs); `components/layout/` **does not exist** (it's `Sidebar.tsx:55`); `getPendingApprovalsCount` **unnecessary** (`listPendingApprovals:1501`); `__STUDIO_BUILD` **67 tags stale and read by nothing**; and the bug the plan never saw — an `environment="production"` default that **no caller threads**, silently mis-addressing every sandbox resume, already worked around inline at `approvals.py:145-148` by a comment that names it. The plan's `App.tsx:87` and `main.tsx:10` citations were checked and are **correct**.
