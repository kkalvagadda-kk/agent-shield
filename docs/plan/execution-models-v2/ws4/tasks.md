# WS-4 Tasks — Event-driven e2e: client-id + allowlist + HMAC webhook signing

**Slice:** WS-4 of Execution Models v2 (spec §5 WS-4; B-series webhook auth; plan `ws4/plan.md`). **Off the WS-0→WS-3 spine** — needs WS-0 only for the `agent_class=daemon` default on event agents; the auth work is standalone and can land in parallel.

**Total tasks:** 30 (24 implementation + 6 checkpoint)
**Phases:** 9 (7 implementation + 2 checkpoint gates)
**Parallel opportunities:** noted inline with `[P]`
**Checkpoint phases:** **Checkpoint 1** (after Phase 5 — real signed dispatch + byte-identical uniform 401 + handler parity; **this is the MVP gate**), **Checkpoint 2** (after Phase 7 — Studio client panel + full suite-76 green + post-impl gates).

> ⚠️ **Re-grounded against the live tree (2026-07-15).** The plan's specifics were **indicative against the 2026-07-12 tree** and it warns they drift (plan §status). Every correction below was verified by reading the live file — **use these, not the plan's numbers**:
>
> | Plan says | Live truth (verified 2026-07-15) |
> |---|---|
> | suite `59` | **TAKEN** — `scripts/e2e/suite-59-workflow-orchestrations-live.sh`. Suites **1..75 all exist**. WS-4 = **`suite-76-webhook-client-signing.sh`**, test IDs **`T-S76-00x`**, registered **after suite-75** (`run-all.sh:124`). |
> | migration `00NN` | Alembic head = **`0063_tools_side_effecting_and_run_eval_mode.py`** (`revision="0063"`). WS-4 = **`0064_webhook_clients.py`**, `down_revision="0063"`. |
> | `POST /api/v1/triggers/{id}/clients` | **NO `/api/v1/triggers` router exists.** `routers/triggers.py` prefix = **`/api/v1/agents`** (`triggers.py:31`) → routes are `/{name}/triggers/{trigger_id}`. Workflow triggers live in `routers/composite_workflows.py` prefix **`/api/v1/workflows`** (`:60`) → `/{workflow_id}/triggers/{trigger_id}` (`:607/:621/:640`). **Resolution: a NEW dedicated router** `routers/webhook_clients.py` mounted at **`/api/v1/triggers`**, keyed on `trigger_id` ALONE — which is exactly the plan's design intent ("clients key on `trigger_id`, and workflow triggers are `agent_triggers` rows with `workflow_id` set"). ONE router serves agent **and** workflow triggers → no per-shape copy (parity). Bolting `/clients` onto both existing routers would have manufactured the exact two-parallel-paths bug class WS-4 is guarding against. |
> | uniform-401 body `{"detail": "unauthorized"}` | **CONFLICTS with live code.** `_uniform_401()` (`main.py:149-151`) already returns **`{"detail": "invalid webhook credentials"}`**. **Keep the live body** (changing it breaks existing token senders' contract for zero security gain) and reuse the existing `_uniform_401()`. **The contract doc must be corrected, not the code.** |
> | (silent) | **PRE-EXISTING 401 ORACLE — WS-4 must close it.** The stale-timestamp path returns a **different** 401 body: `{"detail": "stale webhook timestamp"}` (`main.py:262` workflow, `:366` agent). That is an enumeration oracle today. WS-4's byte-identity assertion **forces** routing stale-ts through `_uniform_401()`. |
> | `AgentDetailPage.tsx` (trigger config) | **WRONG FILE.** Trigger config UI lives in **`studio/src/components/agent-detail/SettingsTab.tsx`** (`WebhookRow` at `:247`, `rotateToken` at `:262`, webhook section at `:88-104`). `AgentDetailPage.tsx` exists but holds no trigger config. Test file = **`SettingsTab.test.tsx`** (exists), NOT `AgentDetailPage.test.tsx`. |
> | bump tags in `deploy-cpe2e.sh` + `charts/agentshield/values.yaml` | **event-gateway's tag is NOT in the top-level values.yaml.** It lives in the **SUB-CHART** `charts/agentshield/charts/event-gateway/values.yaml:6` (`tag: "0.1.1"`); the top-level only carries `event-gateway: enabled: true` (`values.yaml:115-116`). registry-api tag = `values.yaml:590`, studio = `values.yaml:904`. **WS-4 bumps THREE files** (see T021). |
> | `agent_events` shape at `models.py:1673` | **Drift** — `class AgentEvent` is at **`models.py:1741`**; `class AgentTrigger` at **`models.py:1679`** (`token_hash:1710`). No `client_id` column exists → 0064 adds it. |
> | replay nonce store = "deferred, v1 uses the 300s window only" | **PARTIALLY STALE.** Replay protection **already ships** for the token path: `X-Webhook-Timestamp` skew (`main.py:256-264`/`:361-368`) + `X-Webhook-Nonce` via `_rl.check_nonce` (`rate_limiter.py:79`, Redis `SET NX`, fail-closed). The real gap is narrower — it is **opt-in** (only enforced if the sender sends the header) and keyed on **agent_name, not client_id**. Ledger corrected (T023). |
> | data-model `ADD CONSTRAINT IF NOT EXISTS` / `op.create_table(if_not_exists=True)` | **`ADD CONSTRAINT IF NOT EXISTS` is not valid PostgreSQL** (the data-model itself hedges). House style for tables is raw **`op.execute("CREATE TABLE IF NOT EXISTS …")`** (`0036`/`0042`/`0043`/`0044`), and columns via `op.execute("ALTER TABLE … ADD COLUMN IF NOT EXISTS …")` (`0062`). Constraints need a **`DO $$ … pg_constraint …$$`** guard. |
> | (silent) | **Secret storage is under-specified and blocks the gateway.** The gateway must **recompute** `HMAC_SHA256(secret, …)` → it needs the **raw secret**, so a one-way hash cannot work. `crypto.py` (`encrypt_json`/`decrypt_json`, Fernet, `crypto.py:34/39`) is the platform helper, keyed by `AGENTSHIELD_ENCRYPTION_KEY` from the **`agentshield-encryption`** K8s Secret (key `key`; `deploy-cpe2e.sh:308-311`). The event-gateway has **neither** the env var (`charts/agentshield/charts/event-gateway/templates/deployment.yaml:49-79`) **nor** the `cryptography` dep (`requirements.txt` = fastapi/uvicorn/httpx/psycopg2-binary/redis). **T010 wires both** — without it T008 cannot verify a signature. |
>
> **Tags (read from `scripts/deploy-cpe2e.sh:222-230`, do not guess):** registry-api **`0.2.185`**, studio **`0.1.140`**, event-gateway **`0.1.1`**. WS-4 bumps **all three** → `0.2.186` / `0.1.141` / `0.1.2`.

> **NO-FAKES ACCEPTANCE (non-negotiable — CLAUDE.md "No Fakes in E2E").** The 7-defect durable-workflow bug (`docs/bugs/durable-workflow-live-path.md`) proved faked seams hide exactly the bugs living in them — six suites shipped green while the real path was broken. `suite-76` MUST create **real** triggers + **real** clients through the **real** registration API, POST to the **REAL running event-gateway Service** (in-cluster, the same door a real sender hits), and assert **real committed** `agent_events` rows. **NO** mocked httpx, **NO** monkeypatched `verify_webhook_auth`, **NO** hand-crafted `webhook_clients`/`agent_events` rows, **NO** `page.route` stubs in Playwright. Model on **`scripts/e2e/suite-74-eval-v2-side-effects.sh`**. **Fail LOUDLY (not skip)** if a fixture is unreachable — an unprovable gate is never a pass.

> **CRASH-LOUD CENSUS (mandatory — the suite-74 lesson).** suite-74 once reported **"✅ PASSED"** while silently dropping 6 of 11 cases: the driver crashed mid-run, the bare `try/finally` wrote only the cases recorded before the crash, and `PASS>0 FAIL==0` read as green. suite-76 MUST carry **both** guards (T012): (a) a wrapping `except Exception` recording **`T-S76-999 driver ran every case without crashing`** as a **failure** + traceback (`suite-74:714-723`), and (b) an **ID-based completeness census** **`T-S76-COMPLETE`** over a `REQUIRED_IDS` list asserting every expected case actually reported (`suite-74:806-813`). A hardcoded case **count** was tried and immediately drifted — **IDs are the one source of truth**. Use **per-invocation `/tmp` paths** (`RUN_TAG="$(date +%s)$$"`; `suite-74:101-108`) so concurrent runs never cross-report.

> **PARITY GATE (CLAUDE.md "No Bandaid Fixes").** The recurring bug class in this repo is **two parallel paths drifting**: `docs/bugs/side-effecting-lost-on-declarative-runner-path.md` — `side_effecting` was threaded onto the SDK tool-builder but **dropped** by the declarative-runner's own builder, so *every* tool was mocked under record. It survived because the fail-closed default made it *safe*, and a unit test against the *other* path passed. The agent hook (`main.py:314`) and workflow hook (`main.py:199`) are **exactly that shape** — near-identical copies (`_lookup_trigger:74` / `_lookup_workflow_trigger:108` are already duplicated by hand). **`verify_webhook_auth` MUST have exactly 1 definition and 2 call sites.** A per-handler copy is the failure mode to prevent; T017 greps it as a hard assertion.

**MVP scope:** **Checkpoint 1** (Phases 2–5) — a real client registered through the real API signs a real body, the **real** event-gateway verifies it and dispatches, all five failure modes return a **byte-identical** 401, the legacy token path still works under `auth_mode=token`, and **one** `verify_webhook_auth` serves both hooks. Target this first; the Studio panel (CP2) rides on a proven auth hop.

---

## Phase 1 — Setup & Re-grounding
_Establish ground truth before writing code. No behavior change._

- [X] [T001] Record the re-grounding table above into `ws4/plan.md` §status and **correct the two doc-level conflicts in place**: (a) `contracts/webhook-signing.md` §"Uniform-401 body" says `{"detail": "unauthorized"}` — change to the **live** `{"detail": "invalid webhook credentials"}` (`main.py:151`) and note the stale-ts oracle at `:262`/`:366` that WS-4 closes; (b) `contracts/webhook-signing.md` §"Registration API" + plan §5 say `POST /api/v1/triggers/{id}/clients` — record that this is a **NEW router** (`routers/webhook_clients.py`), since no `/api/v1/triggers` prefix exists today. Also record: suite=**76**, migration=**0064**, Studio file=**SettingsTab.tsx**, event-gateway tag lives in the **sub-chart**, secret must be **Fernet-encrypted** (gateway needs the raw secret to recompute the MAC) — `docs/plan/execution-models-v2/ws4/plan.md` + `docs/plan/execution-models-v2/ws4/contracts/webhook-signing.md`
  - **Verify:** `grep -n "suite-76\|0064\|SettingsTab\|invalid webhook credentials" docs/plan/execution-models-v2/ws4/plan.md docs/plan/execution-models-v2/ws4/contracts/webhook-signing.md`

---

## Phase 2 — Schema + ORM (`webhook_clients` + `auth_mode` + `agent_events.client_id`)
_Blocking prerequisite for every later phase. Idempotent, guarded, data-preserving._

- [X] [T002] Create migration **`0064_webhook_clients.py`** (`revision="0064"`, `down_revision="0063"` — head verified). Use the **house style**, NOT the data-model's invalid SQL: (a) `op.execute("ALTER TABLE agent_triggers ADD COLUMN IF NOT EXISTS auth_mode VARCHAR(16) NOT NULL DEFAULT 'token'")` — existing rows keep bearer-token auth; (b) the `auth_mode IN ('token','client_signed')` CHECK inside a **`DO $$ … IF NOT EXISTS (SELECT 1 FROM pg_constraint WHERE conname='ck_agent_triggers_auth_mode') THEN … END IF; $$`** guard (**`ADD CONSTRAINT IF NOT EXISTS` is not valid PostgreSQL** — the data-model's version fails); (c) `op.execute("CREATE TABLE IF NOT EXISTS webhook_clients (…)")` per `data-model.md` — `id UUID PK DEFAULT gen_random_uuid()`, `trigger_id UUID NOT NULL REFERENCES agent_triggers(id) ON DELETE CASCADE`, `client_id VARCHAR(128) NOT NULL`, `secret_encrypted TEXT NOT NULL`, `enabled BOOLEAN NOT NULL DEFAULT true`, `created_by VARCHAR(256)`, `created_at TIMESTAMPTZ NOT NULL DEFAULT now()`, `CONSTRAINT uq_webhook_clients_trigger_client UNIQUE (trigger_id, client_id)` (match `0036`/`0043`'s raw-SQL idiom); (d) `op.execute("ALTER TABLE agent_events ADD COLUMN IF NOT EXISTS client_id VARCHAR(128)")` — the audit reader's column (data-model prefers the column over payload JSON). **Name the column `secret_encrypted`, not `secret_hash`** — it holds a reversible Fernet token and the gateway decrypts it; `secret_hash` would be an actively misleading name for a No-Bandaid review. `downgrade()` drops all four idempotently — `services/registry-api/alembic/versions/0064_webhook_clients.py`
  - **Verify:** `python3 -c "import ast; ast.parse(open('services/registry-api/alembic/versions/0064_webhook_clients.py').read())"` and `grep -n 'down_revision = "0063"' services/registry-api/alembic/versions/0064_webhook_clients.py`
- [X] [T003] Add the ORM: `class WebhookClient(Base)` (`__tablename__="webhook_clients"`, fields mirroring T002 incl. `secret_encrypted: Mapped[str]`, `__table_args__ = (UniqueConstraint("trigger_id","client_id", name="uq_webhook_clients_trigger_client"),)`) placed **next to `class AgentTrigger` (`models.py:1679`)**; add `auth_mode: Mapped[str] = mapped_column(String(16), nullable=False, server_default="token")` to `AgentTrigger` (after `token_hash`, `:1710`); add `client_id: Mapped[str | None] = mapped_column(String(128), nullable=True)` to `class AgentEvent` (**`models.py:1741`** — the data-model's `:1673` is stale) — `services/registry-api/models.py`
  - **Verify:** `python3 -c "import ast; ast.parse(open('services/registry-api/models.py').read())"` (mapper config is verified **on-cluster at CP1b** — `configure_mappers()` canNOT run in the local py3.9 venv, which fails on PEP-604 `Mapped[str | None]`)

---

## Phase 3 — Registration API (secret revealed once) — the gate's producer
_Ships the producer **with** the gate that reads it (plan §4 "no orphan gate" — a required client-id with no way to register it is an outage)._

- [X] [T004] [P] Add the client shapes: `WebhookClientCreate` (`{client_id: str}`, `min_length=1/max_length=128`), `WebhookClientCreatedResponse` (`{client_id, secret, created_at}` — **the only shape that ever carries `secret`**), `WebhookClientResponse` (`{client_id, enabled, created_by, created_at}` — **no `secret` field, structurally**), `WebhookClientUpdate` (`{enabled: bool}`). Make secret-leakage **unrepresentable** rather than filtered at the handler: the read model has no secret field to leak — `services/registry-api/schemas.py`
  - **Verify:** `python3 -c "import ast; ast.parse(open('services/registry-api/schemas.py').read())"` and `grep -n "secret" services/registry-api/schemas.py | grep -i "webhookclient"` shows `secret` **only** on `WebhookClientCreatedResponse`
- [X] [T005] **NEW router** `routers/webhook_clients.py`, `APIRouter(prefix="/api/v1/triggers", tags=["webhook-clients"])` — keyed on `trigger_id` **alone**, so **one** router serves agent AND workflow triggers (`agent_triggers` rows with `workflow_id` set) with zero schema change and **no per-shape copy** (plan §8 "workflow hook is free"). Endpoints per `contracts/webhook-signing.md`: `POST /{trigger_id}/clients` → **201** `WebhookClientCreatedResponse`, generating `secret = "whsec_" + secrets.token_urlsafe(32)`, storing `secret_encrypted = crypto.encrypt_json({"secret": secret})` (`crypto.py:34`) and returning the plaintext **exactly once**; `GET /{trigger_id}/clients` → `list[WebhookClientResponse]` (never the secret); `PATCH /{trigger_id}/clients/{client_id}` (`{enabled}`); `DELETE /{trigger_id}/clients/{client_id}`. Use `get_optional_user` + `AsyncSessionLocal` per `triggers.py:17-35`; stamp `created_by = current_user.sub`; **409** on the `UNIQUE(trigger_id, client_id)` violation; **404** on an unknown `trigger_id` — `services/registry-api/routers/webhook_clients.py`
  - **Verify:** `python3 -c "import ast; ast.parse(open('services/registry-api/routers/webhook_clients.py').read())"`
- [X] [T006] Mount the router — `from routers.webhook_clients import router as webhook_clients_router` next to the `triggers` import (`main.py:70`) + `app.include_router(webhook_clients_router)` next to `:182`. **Without this the whole slice is orphaned** (DoD #3) — `services/registry-api/main.py`
  - **Verify:** `grep -n "webhook_clients_router" services/registry-api/main.py` returns **2** lines (import + include)
- [X] [T007] Set `auth_mode='client_signed'` on **new webhook triggers** at **both** producers — `create_trigger` (`routers/triggers.py:51`, the `trigger_type == "webhook"` branch that already mints `token_hash` at `:64-66`) and the workflow-trigger create in `routers/composite_workflows.py` (the `agent_triggers`-writing producer; WS-2 T008 stamped `armed_by` at these same two sites). Existing rows keep `token` (T002's column default) — dual-mode, **explicit per-trigger, never a try-token-then-fall-back-to-signed priority chain** (plan §2; that fallthrough is the No-Bandaid anti-pattern). Surface `auth_mode` on `AgentTriggerResponse`/`WorkflowTriggerResponse` so the Studio panel (T017) can read the mode — `services/registry-api/routers/triggers.py` + `services/registry-api/routers/composite_workflows.py`
  - **Verify:** `grep -rn "client_signed" services/registry-api/routers/` shows **both** producers; `python3 -c "import ast; [ast.parse(open(f).read()) for f in ['services/registry-api/routers/triggers.py','services/registry-api/routers/composite_workflows.py']]"`

---

## Phase 4 — Gateway shared verify (`webhook_auth.py`) wrapping BOTH hooks
_The parity core. ONE definition, TWO call sites — the anti-drift heart of the slice._

- [X] [T008] Create `webhook_auth.py`: `lookup_webhook_client(trigger_id, client_id) -> dict | None` (psycopg2 `SELECT … FROM webhook_clients WHERE trigger_id=%s AND client_id=%s`, mirroring `main.py:_lookup_trigger:74`'s connection idiom); `sign_webhook(secret, body, ts=None) -> dict` — **the sender reference from `contracts/webhook-signing.md` §"Sender reference", shipped so the e2e suite and real applications sign identically (plan §4/§8: one implementation, no drift)**; and `verify_webhook_auth(trigger, headers, raw_body, path_token) -> WebhookAuthResult` implementing the contract's verify order **exactly**: `auth_mode=="token"` → the existing constant-time `hmac.compare_digest` bearer path; `auth_mode=="client_signed"` → client ∈ allowlist **and** `enabled` → `|now - X-Timestamp| <= 300` → `hmac.compare_digest("sha256="+HMAC_SHA256(secret, f"{ts}.{raw_body}"), X-Signature)`. Decrypt via `crypto.decrypt_json(row["secret_encrypted"])["secret"]`. **Sign/verify the RAW bytes** — never a re-serialized JSON (re-serialization changes bytes and breaks the MAC; contract §headers). **Every** failure returns the SAME sentinel (**no per-reason detail**) so the caller emits one uniform 401 — no enumeration or timing oracle. Return the resolved `client_id` on success for the `agent_events` stamp — `services/event-gateway/webhook_auth.py`
  - **Verify:** `python3 -c "import ast; ast.parse(open('services/event-gateway/webhook_auth.py').read())"` and `grep -c "compare_digest" services/event-gateway/webhook_auth.py` ≥ 1
- [X] [T009] Wire **both** handlers to the ONE `verify_webhook_auth` — `receive_workflow_hook` (**`main.py:199`**, replacing the `_lookup_workflow_trigger` + inline replay block at `:248-269`) and `receive_hook` (**`main.py:314`**, replacing `_lookup_trigger` + `:355-372`). Both accept the new `x_client_id`/`x_timestamp`/`x_signature` headers. **Close the pre-existing 401 oracle:** the stale-timestamp branches (`:262`, `:366`) currently return `{"detail": "stale webhook timestamp"}` — route them through the existing **`_uniform_401()` (`:149-151`, body `{"detail": "invalid webhook credentials"}` — KEEP this body; the contract's `{"detail":"unauthorized"}` is stale and rewriting it would break live token senders for zero gain)**. Stamp the resolved client via `_record_event(..., client_id=…)` (extend the helper's signature + INSERT column list at `:154-191`). Preserve the existing pipeline order — body cap → rate limit → **auth** → filter → dispatch — so rate limiting still counts pre-auth (T-4). **Do NOT copy the verify logic into either handler** (`docs/bugs/side-effecting-lost-on-declarative-runner-path.md`: a second hand-maintained builder silently dropped a field and the fail-closed default hid it) — `services/event-gateway/main.py`
  - **Verify:** `grep -c "verify_webhook_auth" services/event-gateway/main.py` → **2** (both call sites); `grep -c "def verify_webhook_auth" services/event-gateway/webhook_auth.py` → **1**; `grep -n "stale webhook timestamp" services/event-gateway/main.py` → **0 matches** (oracle closed)
- [X] [T010] Give the gateway the ability to decrypt at all — **without this T008 cannot verify a single signature**: add `cryptography==44.0.0` to `services/event-gateway/requirements.txt` (currently fastapi/uvicorn/httpx/psycopg2-binary/redis — **no crypto dep**), and add the `AGENTSHIELD_ENCRYPTION_KEY` env to the gateway Deployment's `env:` block (**`charts/agentshield/charts/event-gateway/templates/deployment.yaml:49-79`**) via `secretKeyRef: {name: agentshield-encryption, key: key}` — the **same** Secret the registry-api already reads (`charts/agentshield/charts/registry-api/templates/deployment.yaml:90-94`), created by `deploy-cpe2e.sh:308-311`. Port `_fernet()`/decrypt into the gateway (small local helper mirroring `crypto.py:24-45` — the services share no Python package) — `services/event-gateway/requirements.txt` + `charts/agentshield/charts/event-gateway/templates/deployment.yaml`
  - **Verify:** `grep -n "cryptography" services/event-gateway/requirements.txt && grep -n "AGENTSHIELD_ENCRYPTION_KEY" charts/agentshield/charts/event-gateway/templates/deployment.yaml`

---

## Phase 5 — suite-76: real fixtures, crash-loud census, parity grep, all auth cases
_One file, appended sequentially (T011→T016). NO FAKES — real API, real gateway, real rows._

- [X] [T011] Scaffold **`suite-76-webhook-client-signing.sh`** — `#!/usr/bin/env bash`, `set -euo pipefail`, `NAMESPACE`/`API_POD`/`ADMIN_SUB="75c7c8b3-7d2d-46e1-8a7b-938dd3c157c6"` (the real interactive platform-admin Keycloak sub — deny-by-default hides resources whose `created_by` ≠ the caller's sub), modeled on **`suite-74`**. **Per-invocation `/tmp` paths** (`suite-74:101-108`): `RUN_TAG="$(date +%s)$$"`, `DRIVER=/tmp/s76_driver_${RUN_TAG}.py`, `OUTFILE=/tmp/s76_out_${RUN_TAG}.txt`, `RUNLOG=/tmp/s76_run_${RUN_TAG}.log` — a fixed path lets two overlapping runs read each other's results. Build the **real** fixture: a real agent authored **`agent_class=daemon`** (`user_delegated` + no live user ⇒ OPA `missing_user_identity` deny — the fixture lesson) using **HTTP tools only** (python-type tools currently crash the pod — known open bug), a real **webhook** trigger (→ `auth_mode='client_signed'` via T007), a real **workflow** + its webhook trigger, and a real client via the **real** `POST /api/v1/triggers/{trigger_id}/clients`. **Fail LOUDLY, never skip**, if any fixture is unreachable — `scripts/e2e/suite-76-webhook-client-signing.sh`
  - **Verify:** `bash -n scripts/e2e/suite-76-webhook-client-signing.sh`
- [X] [T012] Add the **crash-loud + census guards** to suite-76 **before** any case (they are what make every later case trustworthy): (a) wrap the driver body in `except Exception` recording **`T-S76-999 driver ran every case without crashing`** as a **FAIL** + `traceback.format_exc()[-400:]`, re-raising into `finally` so cleanup still runs, and **write the result file BEFORE cleanup** (the suite-69 lesson) — mirror `suite-74:714-727`; (b) after the bash result parse, add the **ID-based** census `REQUIRED_IDS="001 002 003 004 005 006 007 008"` looping `grep -q "T-S76-$id"` → on any miss emit **`FAIL T-S76-COMPLETE every gate assertion ran | NEVER RAN:$MISSING`**, bump `FAIL`, and dump the driver-log tail; mirror `suite-74:806-829`. **Use IDs, never a hardcoded count** — a count drifted the moment suite-74 split 005 into 005a/005b and cannot say *which* case vanished. Exit non-zero on `FAIL≠0` **or** `PASS==0` (inconclusive) — `scripts/e2e/suite-76-webhook-client-signing.sh`
  - **Verify:** `grep -n "T-S76-999\|T-S76-COMPLETE\|REQUIRED_IDS" scripts/e2e/suite-76-webhook-client-signing.sh` shows all three
- [X] [T013] Append **T-S76-000 parity grep** (the anti-drift assertion — run it first, it is cheap and catches the repo's #1 bug class): assert `grep -c "def verify_webhook_auth" services/event-gateway/webhook_auth.py` == **1** (exactly one definition) AND `grep -c "verify_webhook_auth" services/event-gateway/main.py` == **2** (both hook handlers call it) AND `grep -c "def verify_webhook_auth" services/event-gateway/main.py` == **0** (**no per-handler copy** — the `side-effecting-lost-on-declarative-runner-path.md` failure mode) AND `grep -c "stale webhook timestamp" services/event-gateway/main.py` == **0** (the pre-existing oracle stays closed). Any per-handler copy is a parity violation — stop and fix the design, do not special-case — `scripts/e2e/suite-76-webhook-client-signing.sh`
  - **Verify:** `bash scripts/e2e/suite-76-webhook-client-signing.sh 2>&1 | grep "T-S76-000"`
- [X] [T014] Append the **happy paths**, driven against the **REAL running event-gateway Service** in-cluster (`http://agentshield-event-gateway.agentshield-platform.svc.cluster.local:8091`) — never a mocked transport: **T-S76-001** valid signed POST to the **agent** hook `/hooks/{name}/{token}` (headers from the **shipped `sign_webhook` reference**, T008 — the suite imports the same helper real senders use, so a drift between test and product is impossible) → **202 accepted** + a **real committed** `agent_events` row with `status='matched'` **and `client_id` = the registered client** (re-read **from the DB**, not the response). **T-S76-002** the same signed flow against the **workflow** hook `/hooks/workflow/{name}/{token}` → 202 + real `agent_events` row with `workflow_id` + `client_id` set — **this is the parity payoff**: the workflow hook gets signing with zero schema change (plan §8) — `scripts/e2e/suite-76-webhook-client-signing.sh`
  - **Verify:** `bash scripts/e2e/suite-76-webhook-client-signing.sh 2>&1 | grep -E "T-S76-00[12]"`
- [X] [T015] Append **T-S76-003 — UNIFORM 401 BYTE-IDENTITY (the security property).** Drive **all five** failure modes against the real gateway: (1) unknown client-id, (2) bad signature, (3) stale timestamp (`ts = now-600`, **the branch that returned a distinct body at `main.py:262`/`:366` before T009**), (4) disabled client (real `PATCH …/clients/{client_id}` `{"enabled":false}`), (5) wrong-trigger client (a client registered on trigger **A** signing for trigger **B** — the allowlist boundary). Assert **status == 401 for all five** AND — the load-bearing part — that the five response bodies are **BYTE-IDENTICAL to each other** (compare the raw `resp.content`, e.g. `len({r.content for r in five}) == 1`), not merely "each is 401". A distinct body per reason is an **enumeration oracle** that tells an attacker *which* check failed; asserting only the status code would let that regress silently. Also assert the body equals the live `_uniform_401()` body `{"detail":"invalid webhook credentials"}` — `scripts/e2e/suite-76-webhook-client-signing.sh`
  - **Verify:** `bash scripts/e2e/suite-76-webhook-client-signing.sh 2>&1 | grep "T-S76-003"`
- [X] [T016] Append the **dual-mode + allowlist** cases: **T-S76-004** a trigger explicitly at `auth_mode='token'` still accepts the **legacy bearer token** with **no** signing headers → 202 + real `agent_events` row (backward compat — existing senders must not break mid-migration); **T-S76-005** a `client_signed` trigger **rejects** that same legacy bare token (401 uniform) — proving the mode is an **explicit branch, not a fallthrough** (if a bare token succeeds on a `client_signed` trigger, someone reintroduced the priority-chain anti-pattern); **T-S76-006** registration returns the secret **exactly once** — the `POST` 201 carries `secret`, and a subsequent `GET …/clients` **never** does (assert the key is absent from the JSON, not merely empty); **T-S76-007** `UNIQUE(trigger_id, client_id)` is enforced — a duplicate `client_id` on the same trigger → **409**, while the **same** `client_id` on a **different** trigger **succeeds** (the allowlist is per-trigger); **T-S76-008** a **re-enabled** client (`PATCH {"enabled":true}`) is accepted again → 202, proving disable/enable is a live allowlist read, not a cached decision — `scripts/e2e/suite-76-webhook-client-signing.sh`
  - **Verify:** `bash scripts/e2e/suite-76-webhook-client-signing.sh 2>&1 | grep -E "T-S76-00[45678]"`

---

## Checkpoint 1 — Real signed dispatch + uniform 401 + handler parity (**MVP GATE**)
_Gate: Phases 2–5 complete. Run before starting Phase 6._
_What you prove: a real client registered through the real API signs a real body; the **real** event-gateway verifies, dispatches, and stamps `client_id` on a **real** `agent_events` row — on **both** the agent and workflow hooks from **one** `verify_webhook_auth`; all five failure modes return a **byte-identical** 401; and a legacy `token` trigger still works._

- [X] [CP1a] Deploy script `scripts/deploy-cp1-ws4.sh` — thin idempotent wrapper: echo scope → **`bash scripts/deploy-cpe2e.sh`** → `kubectl rollout status deploy/agentshield-event-gateway` + `deploy/agentshield-registry-api`. Builds **event-gateway `0.1.2`** (T008/T009/T010) + **registry-api `0.2.186`** (T002–T007). The wrapper **delegates to `deploy-cpe2e.sh` and NEVER runs bare helm/docker/kubectl for the deploy** (CLAUDE.md "Deploy Script Only" — a code edit that isn't built+deployed leaves the pod on OLD code). Requires T021's tag bumps to be committed first — `scripts/deploy-cp1-ws4.sh`
  - **Verify:** `bash scripts/deploy-cp1-ws4.sh`
- [X] [CP1b] Infra smoke `scripts/smoke-test-cp1-ws4-infra.sh` — REAL `kubectl`/`psql`/`httpx` assertions, `exit 0` only on all-pass: **T-CP1B-001** event-gateway pods `Running` on image tag **`0.1.2`** (`kubectl get pod -o jsonpath` over `.spec.containers[].image`), crashloop=0; **T-CP1B-002** registry-api pods `Running` on **`0.2.186`**, crashloop=0; **T-CP1B-003** alembic head == **`0064`** (`kubectl exec` → `alembic current`); **T-CP1B-004** schema landed — `webhook_clients` exists with `uq_webhook_clients_trigger_client`, plus `agent_triggers.auth_mode` and `agent_events.client_id` columns (`information_schema` queries, HTTP-status-checked); **T-CP1B-005** the **mapper-config gate** — `kubectl exec` into the registry-api pod and run `configure_mappers()` **from a WRITABLE temp dir** (`cd /tmp && PYTHONPATH=/app python3 -c "import routers.webhook_clients, models; from sqlalchemy.orm import configure_mappers; configure_mappers(); print('ok')"`). **This MUST run on-cluster** — the local py3.9 venv fails on PEP-604 `Mapped[str | None]`, and `/app` is **read-only** so a bare `cd /app` exec cannot write `__pycache__`; **T-CP1B-006** the gateway can decrypt — `AGENTSHIELD_ENCRYPTION_KEY` is present in the event-gateway pod env and `cryptography` imports (`kubectl exec` → `python3 -c "import cryptography; import os; assert os.environ['AGENTSHIELD_ENCRYPTION_KEY']"`) — without this every signature verification fails at runtime — `scripts/smoke-test-cp1-ws4-infra.sh`
  - **Verify:** `bash scripts/smoke-test-cp1-ws4-infra.sh`
- [X] [CP1c] Behaviour smoke `scripts/smoke-test-cp1-ws4-behaviour.sh` — runs **`bash scripts/e2e/suite-76-webhook-client-signing.sh`** (T-S76-000..008 + T-S76-999 + T-S76-COMPLETE) and asserts all green: parity grep (1 def / 2 call sites / 0 copies / 0 oracle); real signed → 202 + real `agent_events.client_id` on **both** hooks; **five failure modes byte-identical 401**; legacy token still 202; secret-once; UNIQUE 409. Explicit `jq`/HTTP-status checks; `exit 0` only on all-pass. **This IS the no-fakes gate** — `scripts/smoke-test-cp1-ws4-behaviour.sh`
  - **Verify:** `bash scripts/smoke-test-cp1-ws4-behaviour.sh`

> **To run:** `bash scripts/deploy-cp1-ws4.sh` → wait for pods → `bash scripts/smoke-test-cp1-ws4-infra.sh && bash scripts/smoke-test-cp1-ws4-behaviour.sh`
> **Pass criteria:** all assertions exit 0; no pod in CrashLoopBackOff; alembic head `0064`; `verify_webhook_auth` = 1 def / 2 call sites / **0** per-handler copies; the five 401 bodies are byte-identical; `T-S76-COMPLETE` confirms every case reported.

---

## Phase 6 — Studio client panel (register / reveal-once / enable-disable / audit)
_The producer's UI — ships with the gate (plan §4). Note the file drift: this is **`SettingsTab.tsx`**, not the plan's `AgentDetailPage.tsx`._

- [X] [T017] [P] Add the API client methods next to the existing trigger block (`registryApi.ts:1178-1240`): `createTriggerClient(triggerId, {client_id})` → `POST /triggers/{triggerId}/clients` (returns `{client_id, secret, created_at}` — type the **`secret` as the create-only field**, mirroring how `webhook_url` is already typed "Populated ONLY in the create response (shown once)" at `:1193-1195`), `listTriggerClients(triggerId)`, `setClientEnabled(triggerId, clientId, enabled)` → `PATCH`, `deleteTriggerClient(triggerId, clientId)` → `DELETE`. Add `auth_mode` + `approver_role`-style fields to the `AgentTrigger` interface (`:1180`) so the panel can read the mode. **Every method needs a live caller in T018** — an API-client method with no caller is orphan debt (DoD #3) — `studio/src/api/registryApi.ts`
  - **Verify:** `cd studio && npm run typecheck`
- [X] [T018] Extend **`WebhookRow`** (`SettingsTab.tsx:247`, which already does exactly this shape for `rotateToken` at `:262-268` — **reuse its reveal-once + copy-to-clipboard idiom**, do not invent a second one) with a **client registration panel**: list registered clients (`listTriggerClients`), an "Add client" control (`createTriggerClient`) that **reveals the secret exactly once** in a copy-able callout with a "copy it now — it won't be shown again" warning (mirroring `:265`), an enable/disable toggle per client (`setClientEnabled`), a delete control, and a per-client audit line (`created_by` + `created_at`). Show the trigger's `auth_mode` so an operator can see whether a trigger is `token` or `client_signed`. Invalidate the query on every mutation (the `invalidate` idiom at `:34`) — `studio/src/components/agent-detail/SettingsTab.tsx`
  - **Verify:** `cd studio && npm run typecheck` and `grep -rn "createTriggerClient\|listTriggerClients\|setClientEnabled\|deleteTriggerClient" studio/src` shows a **live caller** for each (orphan grep, DoD #3)
- [X] [T019] [P] Extend the colocated Vitest for the new states (mock via `vi.mock('../../api/registryApi')`, render via `renderWithProviders` from `src/test/utils.tsx`): the client list renders; "Add client" calls `createTriggerClient` and the returned **secret is revealed once**; a **re-render/list refetch does NOT show the secret again**; the disable toggle calls `setClientEnabled(false)`; the empty (no clients) state; and the `auth_mode=token` vs `client_signed` display — `studio/src/components/agent-detail/SettingsTab.test.tsx`
  - **Verify:** `cd studio && npm run test`
- [X] [T020] [P] Playwright **`webhook-clients.spec.ts`** — real Keycloak login (`e2e/global-setup.ts`), against the **https gateway `https://agentshield.127.0.0.1.nip.io:8443`** (Secure Keycloak cookies break over an http port-forward). **NO `page.route` stubs** — bug #7's lesson is that a stubbed route proves nothing about the real path. Drive: navigate to a webhook trigger's Settings → register a client (`page.waitForResponse` on `POST **/triggers/*/clients`) → assert the **secret renders once** → **save → reload → assert survived**: reload the page from the backend and assert the **client row persists** while the **secret is gone** (DoD #2 — the persistence round-trip; this surface writes data, so the guard is mandatory) → toggle disable (`waitForResponse` on the `PATCH`) → reload → assert `enabled=false` **survived**. Assert UI wiring + persistence + network calls, not agent execution — `studio/e2e/webhook-clients.spec.ts`
  - **Verify:** `bash scripts/studio-e2e.sh`

---

## Phase 7 — Post-impl gates (register suite, bump tags, docs, gap ledger, orphan sweep)
_Five independent files → parallelizable. Closes the CLAUDE.md Post-Implementation Checklist._

- [X] [T021] [P] Bump **THREE** files (not two — the plan's/CLAUDE.md's "both files" rule has a **sub-chart exception** for event-gateway): (1) `scripts/deploy-cpe2e.sh` — `REGISTRY_API_TAG` `0.2.185→0.2.186` (`:222`), `STUDIO_TAG` `0.1.140→0.1.141` (`:225`), `EVENT_GATEWAY_TAG` `0.1.1→0.1.2` (`:230`), plus a WS-4 comment-header entry (`:1-20` idiom); (2) `charts/agentshield/values.yaml` — registry-api tag `:590` → `0.2.186`, studio tag `:904` → `0.1.141`; (3) **`charts/agentshield/charts/event-gateway/values.yaml:6`** — `tag: "0.1.1"` → `"0.1.2"`. **The top-level `values.yaml` does NOT carry the event-gateway tag** (only `event-gateway: enabled: true` at `:115-116`) — bumping only the top two would silently deploy the **old** gateway image and every signing case would fail against stale code. The deploy uses `helm upgrade` with tags baked into values (no `--set`) — `scripts/deploy-cpe2e.sh` + `charts/agentshield/values.yaml` + `charts/agentshield/charts/event-gateway/values.yaml`
  - **Verify:** `grep -n "0.2.186\|0.1.141" scripts/deploy-cpe2e.sh charts/agentshield/values.yaml && grep -n '0.1.2' scripts/deploy-cpe2e.sh charts/agentshield/charts/event-gateway/values.yaml`
- [X] [T022] [P] Register suite-76 in the runner **after suite-75** (`run-all.sh:124`) — add the `run_suite "Suite 76: Webhook client-id + HMAC signing (no-fakes)" "suite-76-webhook-client-signing.sh"` line and include it in the run loop / totals — `scripts/e2e/run-all.sh`
  - **Verify:** `grep -n "suite-76" scripts/e2e/run-all.sh`
- [X] [T023] [P] Document the webhook client-signing UX (**mandatory** — `routers/*.py` + Studio trigger-config changes are covered files under CLAUDE.md §3): the per-application client-id + allowlist model, the `X-Client-Id`/`X-Timestamp`/`X-Signature` wire contract, the **secret-revealed-once** registration flow, enable/disable as a live allowlist, the **uniform 401** (no reason is ever disclosed — by design), the dual-mode `token`→`client_signed` migration story, and the event log showing the resolved `client_id`. Mirror the existing daemon/`armed_by` prose style — `docs/experience/playground.md`
  - **Verify:** `grep -n "X-Client-Id\|client_signed" docs/experience/playground.md`
- [X] [T024] [P] Record the **WS-4 gap ledger** in the canonical place (`docs/testing/manual-ui-e2e-test-plan.md` header), tagged **deferred (intentional)** vs **not-yet-wired (debt)** per plan §7 — **with the nonce entry corrected against live code** (see the ledger table below; the plan's "v1 uses the 300s window only" is stale — `_rl.check_nonce` already ships). Never let an unfinished piece read as shipped (DoD #5) — `docs/testing/manual-ui-e2e-test-plan.md`
  - **Verify:** `grep -n "WS-4" docs/testing/manual-ui-e2e-test-plan.md`
- [X] [T025] Orphan-grep + verification sweep — for **every** new symbol confirm a live caller/reader (DoD #3; "build utility + not wiring it up = NOT done"): `grep -rn "verify_webhook_auth\|sign_webhook\|lookup_webhook_client" services/event-gateway` (readers = both hooks + the suite's sender), `grep -rn "webhook_clients_router" services/registry-api/main.py` (mounted, else the whole API is orphaned), `grep -rn "createTriggerClient\|listTriggerClients\|setClientEnabled\|deleteTriggerClient" studio/src` (each has a `SettingsTab` caller), `WebhookClient`/`auth_mode`/`agent_events.client_id` (producer = registration API + trigger create + verify; reader = gateway branch + event log UI). Run the full verification set: `python3 -c "import ast; ast.parse(...)"` over every touched `.py`; **`configure_mappers()` on-cluster** (CP1b T-CP1B-005 — **not locally**, the py3.9 venv fails on PEP-604 `Mapped[str | None]`, and it must run from a **writable** temp dir since `/app` is read-only); `cd studio && npm run typecheck && npm run test`. Record results in — `docs/plan/execution-models-v2/ws4/plan.md`
  - **Verify:** `cd studio && npm run typecheck && npm run test` and the grep set above returns a caller for **every** symbol

---

## Checkpoint 2 — Studio panel + full suite-76 green + post-impl gates
_Gate: Phases 6–7 complete. Final WS-4 gate._
_What you prove: an operator registers a client in the real browser, sees the secret once, and it persists across a reload; disabling it makes the **real** gateway 401; the full suite-76 is green and registered; all three tag files are bumped._

- [X] [CP2a] Deploy script `scripts/deploy-cp2-ws4.sh` — thin wrapper: echo scope → **`bash scripts/deploy-cpe2e.sh`** (builds + deploys **studio `0.1.141`** for the T018 panel, alongside the already-rolled event-gateway `0.1.2` / registry-api `0.2.186`) → `kubectl rollout status deploy/agentshield-studio`. **Delegates to `deploy-cpe2e.sh`; never bare helm/docker/kubectl** — `scripts/deploy-cp2-ws4.sh`
  - **Verify:** `bash scripts/deploy-cp2-ws4.sh`
- [X] [CP2b] Infra smoke `scripts/smoke-test-cp2-ws4-infra.sh` — REAL assertions: **T-CP2B-001** studio pod `Running` on image tag **`0.1.141`** (`kubectl get pod -o jsonpath`), crashloop=0; **T-CP2B-002** the registration API is **mounted and serving** — `POST /api/v1/triggers/{id}/clients` on a real trigger returns **201** (not 404 — a 404 means T006 never mounted the router), and `GET` returns the client **without** a `secret` key; **T-CP2B-003** suite-76 is registered (`grep -n "suite-76" scripts/e2e/run-all.sh`); **T-CP2B-004** all three tags present in their **three** files — `0.2.186`+`0.1.141` in `deploy-cpe2e.sh` and `charts/agentshield/values.yaml`, and **`0.1.2` in `charts/agentshield/charts/event-gateway/values.yaml`** (the sub-chart — the one a "bump both files" habit silently misses) — `scripts/smoke-test-cp2-ws4-infra.sh`
  - **Verify:** `bash scripts/smoke-test-cp2-ws4-infra.sh`
- [X] [CP2c] Behaviour smoke `scripts/smoke-test-cp2-ws4-behaviour.sh` — `cd studio && npm run typecheck && npm run test` (Vitest incl. `SettingsTab.test.tsx`) green; **`bash scripts/studio-e2e.sh`** runs `webhook-clients.spec.ts` (register → secret-once → **reload → persisted, secret gone** → disable → reload → survived) green against the **https gateway**; then **`bash scripts/e2e/suite-76-webhook-client-signing.sh`** **fully green** (T-S76-000..008 + `T-S76-COMPLETE`). `exit 0` only on all-pass — `scripts/smoke-test-cp2-ws4-behaviour.sh`
  - **Verify:** `bash scripts/smoke-test-cp2-ws4-behaviour.sh`

> **To run:** `bash scripts/deploy-cp2-ws4.sh` → `bash scripts/smoke-test-cp2-ws4-infra.sh && bash scripts/smoke-test-cp2-ws4-behaviour.sh` → `bash scripts/studio-e2e.sh` (Playwright, separate gate)
> **Pass criteria:** studio on `0.1.141`; suite-76 all-green + `T-S76-COMPLETE` + registered; typecheck + Vitest + Playwright green; three tag files bumped.

---

## Summary Table

| Phase | Type | Tasks | Proves / Delivers |
|---|---|---|---|
| 1 — Setup & Re-grounding | impl | T001 | Ground truth recorded; the two doc-level conflicts (401 body, route prefix) corrected in the contract |
| 2 — Schema + ORM | impl | T002, T003 | `0064` `webhook_clients` + `auth_mode` + `agent_events.client_id`; `WebhookClient` mapper |
| 3 — Registration API | impl | T004–T007 | Secret-once producer on a NEW `/api/v1/triggers` router serving agent **and** workflow triggers; `auth_mode` set at both create producers |
| 4 — Gateway shared verify | impl | T008–T010 | ONE `verify_webhook_auth` + sender reference; both hooks wired; 401 oracle closed; gateway can decrypt |
| 5 — suite-76 (no-fakes) | impl | T011–T016 | Crash-loud + ID census; parity grep; real signed 202 on both hooks; **byte-identical** 401 ×5; dual-mode; secret-once; UNIQUE |
| **Checkpoint 1 (MVP)** | gate | CP1a, CP1b, CP1c | Deploy (gw `0.1.2` + api `0.2.186`) + infra (head `0064`, on-cluster mappers, decrypt-capable) + the no-fakes behaviour gate |
| 6 — Studio client panel | impl | T017–T020 | `SettingsTab` register/reveal-once/enable-disable/audit + Vitest + Playwright save→reload |
| 7 — Post-impl gates | impl | T021–T025 | Three-file tag bump; suite-76 registered; playground.md; gap ledger; orphan sweep |
| **Checkpoint 2** | gate | CP2a, CP2b, CP2c | Studio `0.1.141` + router-mounted + full suite-76 green + typecheck/Vitest/Playwright |

**Parallel opportunities:** T004 `[P]` (schemas, independent of the migration); T017 `[P]` + T019 `[P]` + T020 `[P]` (API client, test file, spec file — distinct files from the `.tsx` edit); T021–T024 `[P]` (four independent file groups). Same-file appends to `suite-76` (T011→T012→T013→T014→T015→T016) are **sequential** (one file). Phase 2 → 3 → 4 is a hard chain (schema → producer → reader); **T010 must land with T008** or the gateway cannot decrypt at runtime.

---

## Gap Ledger (carried from plan §7, **re-grounded** against live code)

| Item | Status | Note |
|---|---|---|
| Replay **nonce** store **scoped to `client_id`** | **deferred (intentional) → future** | **The plan's entry ("v1 uses the 300s timestamp window only") is STALE.** Replay protection already ships for the token path: `X-Webhook-Timestamp` skew (`main.py:256-264`/`:361-368`) + `X-Webhook-Nonce` via `_rl.check_nonce` (`rate_limiter.py:79`, Redis `SET NX`, fail-closed). The **real, narrower** gap: the nonce check is **opt-in** (enforced only if the sender sends the header) and keyed on **`agent_name`, not `client_id`**. So a signed request replayed **inside the 300s window** by a client that omits the nonce header is accepted. Closing it = make the nonce mandatory for `client_signed` and key it on `(trigger_id, client_id, nonce)`. |
| Per-client rate limits | **deferred (intentional) → future** | Rate limiting stays **per-agent + per-source-IP** (the existing `rate_limiter`, `main.py:239`/`:346`, still evaluated **before** auth so it counts on 401s — T-4). A noisy client cannot be throttled independently of its trigger's other clients. |
| Auto-migrate all senders off `token` + **delete the `auth_mode` flag** | **not-yet-done (debt, intentional)** | Dual-mode ships by design (existing senders must not break). The flag is removed only once every sender is on `client_signed`. Until then `auth_mode='token'` triggers remain on the coarse per-trigger bearer token — **the pre-WS-4 security posture, per-trigger not per-app**. |
| `contracts/webhook-signing.md` uniform-401 body | **corrected in T001 (doc bug, not code debt)** | The contract's `{"detail": "unauthorized"}` never matched the live `_uniform_401()` (`main.py:151`). The **code** is authoritative; the doc is corrected. |

**No orphan flags** — every producer ships with its reader in this slice (plan §7): `webhook_clients` (producer = registration API T005 + Studio panel T018; reader = gateway verify T008), `auth_mode` (producer = trigger create T007; reader = gateway branch T009), `agent_events.client_id` (producer = verify/`_record_event` T009; reader = the event log UI + suite-76 T014). T025 greps a live caller for each before WS-4 is reported done.

---

## Definition of Done (WS-4)

- **Real user journey proven (DoD #1):** `studio/e2e/webhook-clients.spec.ts` (T020) drives register → secret-once → disable in a real browser against the https gateway, **no `page.route` stubs**. suite-76 drives the **real** event-gateway, not an endpoint mock.
- **Save → reload → assert (DoD #2):** T020 registers a client, waits on the POST, **reloads from the backend**, and asserts the client row survived **and the secret is gone**; then disables, reloads, asserts `enabled=false` survived.
- **No orphans (DoD #3):** T006 mounts the router (else the entire API is dead code); T025 greps a live caller for `verify_webhook_auth`, `sign_webhook`, `lookup_webhook_client`, all four `registryApi` methods, `WebhookClient`, `auth_mode`, `agent_events.client_id`.
- **Vertical slice (DoD #4):** schema → registration API → gateway verify → real signed dispatch is proven at **CP1** before the Studio panel starts. The gate's **producer ships with the gate** (plan §4) — a required client-id with no way to register it would be an outage.
- **Honest gap ledger (DoD #5):** above — nonce-scoping + per-client rate limits = **deferred (intentional)**, `auth_mode` flag deletion = **debt (intentional)**; recorded in `docs/testing/manual-ui-e2e-test-plan.md` (T024).
- **Reason from the running product (DoD #6):** every plan specific was re-verified against live code — suite 59→**76**, migration →**0064**, the route prefix that **does not exist**, the Studio file that is **`SettingsTab.tsx`**, the event-gateway tag in a **sub-chart**, the 401 body **conflict**, the **stale-ts oracle** the plan never noticed, and the nonce gap the plan **overstated**. The plan's `main.py:199`/`:314`/`:99`/`:136` citations were checked and are **correct**.
